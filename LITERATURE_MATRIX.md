# Literature Matrix

This file is the staging area before writing into [main.tex](main.tex). Each row should map one paper to the survey scaffold.

## How To Use

1. Add one row per paper.
2. Fill all columns before editing prose.
3. Group rows by target section/subsection.
4. Mark `Status` as `ready` only when the row can be converted into synthesis text.

## Status Values

- `todo`: paper selected but not read
- `reading`: paper being read
- `note-ready`: scaffold fields filled
- `ready`: can be integrated into manuscript prose
- `integrated`: already integrated into manuscript

## Matrix

| Bib key | Area | State object | Control surface | Coupling path | Evaluation boundary | Remaining gap | Target subsection in main.tex | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| kwon2023pagedattention | LLM serving | per-request KV pages | paging, sharing, and lifecycle admission | fragmentation and batch-size coupling | throughput at latency target | cross-node lifecycle governance | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| agrawal2024sarathi | LLM serving | prefill/decode working set | prefill/decode scheduling and batch shaping | phase asymmetry and queueing | throughput-latency tradeoff | cluster-scale disturbance-aware phase control | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| crankshaw2017clipper | serving orchestration | model/request serving state | serving orchestration and fallback control | latency/SLO tradeoff under online serving | latency + accuracy + availability | reusable memory and phase lifecycle remain implicit | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| gujarati2020clockwork | predictable serving | schedule and execution-time state | predictability-driven dispatch and reservation control | interference and latency uncertainty | tail latency/SLO isolation | short-lived memory governance remains weak | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| yu2022orca | LLM serving | partially materialized decode state | iteration-level scheduling and selective batching | head-of-line blocking and batch inefficiency | throughput + tail latency | memory lifecycle remains implicit | State-Aware Execution Optimization / KV-Cache Management | TBD | integrated |
| kim2025oaken | KV-cache compression | per-head KV-cache tensors plus dense/sparse mixed-precision metadata | offline threshold selection, group-shift quantization, and DMA-integrated quantize/dequantize control | KV precision reshapes HBM capacity, bandwidth pressure, and regularity of cache access on the serving path | throughput + accuracy loss + hardware area/power under accelerator-integrated serving | calibration drift, hardware specificity, and weak coupling to admission/eviction/tiering policy | State-Aware Execution Optimization / KV-Cache Management | TBD | note-ready |
| kumar2025aqa | LLM serving / remote-GPU offloading | tensor-level inference working set and offloaded residency mappings across local and remote HBM | producer-consumer role selection, NVLink-domain placement, and tensor-level remote-memory scheduling | remote HBM borrowing relieves local memory pressure and reshapes queueing, TTFT, and long-prompt throughput under scale-up serving | TTFT + completion time + throughput under single-node NVLink domains | only applies within one NVLink domain, with weak cross-node ownership, isolation, and degradation semantics | State-Aware Execution Optimization / KV-Cache Management | TBD | note-ready |
| jiang2025neo | online LLM inference offloading | decode attention working set and request-level KV-cache residency across GPU HBM and CPU DRAM | asymmetric CPU-GPU pipelining, swap in/out, CPU sub-batch sizing, and load-aware offload scheduling | KV offload frees GPU batch capacity, and batch size in turn determines utilization, throughput, and latency | throughput-latency frontier under online serving and bounded GPU memory | greedy scheduling, single-node assumptions, and weak integration with chunked prefill and cross-GPU coordination | State-Aware Execution Optimization / KV-Cache Management | TBD | note-ready |
| lin2025qserve | low-bit LLM serving / KV-cache quantization | quantized weights plus 4-bit KV cache and dequantization/layout metadata | progressive quantization, compute-aware weight reordering, and KV4 attention path optimization | dequantization overhead and KV precision reshape batch-size feasibility, memory traffic, and serving throughput on the GPU serving path | end-to-end serving throughput + cost efficiency under large-batch GPU serving | still weak on cross-request KV lifecycle governance, admission/eviction/tiering, and portability across hardware generations | State-Aware Execution Optimization / KV-Cache Management | TBD | note-ready |
| crankshaw2020inferline | serving control plane | pipeline queues and provisioning state | provisioning, autoscaling, and admission control | workload forecasting and SLO drift | cost/SLO frontier | weaker coupling to short-lived memory layers | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| shen2019nexus | cluster serving | GPU occupancy and pipeline queue state | cluster occupancy, admission, and batching control | GPU contention and stage imbalance | throughput/latency in pipelines | short-lived memory lifecycle still implicit | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| romero2021infaas | serving automation | replica and model-placement state | autoscaling, replica selection, and placement control | workload shifts and heterogeneous replica cost | latency/cost under dynamic demand | weak coupling to reusable KV and adapter layers | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| sheng2023flexgen | memory-constrained serving | offloaded model and KV state | tiered offload and memory-plane placement control | single-GPU memory pressure and transfer overhead | throughput under memory limits | weaker cluster-scale lifecycle governance | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| yang2025lserve | long-context sparse LLM serving | KV-cache pages, streaming-versus-dense head partitions, logical/physical page mappings, and page-importance statistics | unified sparse attention, hierarchical KV page selection, token budgets, and page-selection reuse intervals | sparse head choice and KV page selection jointly reshape prefill cost, decode I/O, and long-context serving latency | prefill speed + decode speed + TTFT + long-context accuracy under long-sequence serving | still weak on explicit memory-capacity governance, cross-node KV ownership, and compatibility with broader serving features | State-Aware Execution Optimization / KV-Cache Management | TBD | note-ready |
| zhong2024distserve | serving disaggregation | separated prefill/decode state paths | phase disaggregation and cluster admission control | cross-stage queueing and transfer cost | goodput/tail latency | tenant-aware governance across phases remains weak | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| patel2024splitwise | serving disaggregation | split prefill/decode state lifecycle | phase splitting, placement, and transfer control | phase imbalance and resource contention | throughput-latency | generalized governance under multi-tenant phase mixing | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| holmes2024fastgen | LLM serving | fused/split prompt-generation working set | dynamic phase fusion, splitting, and queue control | long-prompt pressure and queue imbalance | average + tail latency | cluster-scale transfer and admission governance | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| qin2026prfaas | serving disaggregation | cross-cluster prefill outputs and transferable KV cache | selective prefill offload, bandwidth-aware scheduling, and cache-aware placement | phase separation becomes cross-cluster only when burst, skew, and bandwidth variation are jointly managed | throughput + TTFT under heterogeneous deployment | explicit global admission and ownership semantics remain weak | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| guo2026splitzip | KV transfer optimization | transferable KV tensors in disaggregated serving | GPU-friendly lossless KV compression on the transfer path | inter-stage transport becomes the bottleneck once prefill/decode are split | transfer latency + TTFT + request throughput | compression still lacks unified coupling with admission and restoration scheduling | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| nian2026cacheflow | KV restoration scheduling | restorable KV cache across tokens, layers, and GPUs | batch-aware restoration scheduling with compute/I/O overlap | restoration policy reshapes TTFT under long-context, RAG, and agentic workloads | TTFT under contention and batched serving | restoration remains weakly integrated with phase placement and long-horizon lifecycle control | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| windserve2025 | serving disaggregation | split prefill/decode KV-cache state, request placement, and decode-instance load state | dynamic prefill dispatch, dynamic rescheduling, and stream-based KV migration | prompt-length skew changes prefill completion order, decode imbalance, and migration pressure across disaggregated pools | TTFT + TPOT tail latency + throughput under disaggregated multi-GPU serving | depends on topology-specific migration efficiency and leaves adaptive pool sizing and stronger ownership semantics open | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| guo2025gllm | distributed LLM serving / pipeline parallelism | micro-batch-level prefill/decode token counts, queue backlog, and KV-cache free-space state across active pipeline stages | token throttling over prefill token count and decode token allocation, plus asynchronous driver-worker metadata scheduling | prefill and decode contend for compute slots and KV-cache capacity, so token-count imbalance directly amplifies pipeline bubbles and SLO drift | throughput + TTFT/TPOT/E2EL + SLO attainment under distributed serving workloads | hyperparameter tuning remains manual, low-load TTFT can regress slightly, and the current design only addresses inter-batch rather than inter-stage bubbles | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| gong2025pastfuture | SLA-aware LLM serving | per-request predicted output length and future KV-cache memory occupancy timeline | history-based length sampling, future-peak memory estimation, and admission control with reserved headroom | output-length prediction reshapes batch admission, eviction risk, queueing, and goodput under continuous batching | goodput under P99 TTFT/MTPOT SLA + memory utilization + eviction rate | fixed reserved headroom and single-node assumptions leave adaptive uncertainty control and cluster-wide scheduling open | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| mo2025hetis | heterogeneous LLM serving | module-specific compute state, head-grain attention assignments, and head-local KV-cache residency/load state across heterogeneous GPUs | dynamic master-worker selection, head-grain attention parallelism, and KV-aware runtime load redistribution | compute-memory heterogeneity and KV placement jointly shape pipeline imbalance, communication cost, and tail latency | throughput + end-to-end latency + SLO attainment in heterogeneous GPU clusters | communication overhead, profiling dependence, and weak lifecycle control beyond attention-head state | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| su2025seesaw | phase-adaptive LLM inference parallelism | dynamically re-sharded model weights, hierarchical KV-cache buffers, and phase-specific parallelism state | prefill/decode phase switching between pipeline and tensor parallelism, hierarchical KV buffering, and transition-minimizing scheduling | phase-specific parallelism and resharing cost jointly determine throughput under resource-constrained multi-GPU inference | offline inference throughput on PCIe-connected GPU clusters | CPU-GPU transfer overhead and applicability beyond throughput-oriented offline settings remain weak | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| jiang2025thunderserve | heterogeneous-cloud LLM serving | split prefill/decode execution state, compressed transferable KV cache, and heterogeneity-aware service-group placement state | two-level GPU-group search, phase-specific GPU assignment, one-shot KV compression, and lightweight role re-scheduling | compute-versus-memory asymmetry and cloud GPU price/performance heterogeneity jointly shape throughput, SLO attainment, and transfer overhead | throughput per budget + SLO attainment under heterogeneous cloud deployments | benefits weaken when sequences are too short for split-phase specialization to amortize transfer cost and leave limited room for stronger cross-cluster ownership semantics | LLM Serving and Short-Lived Memory Lifecycles | jiang2025thunderserve | note-ready |
| chen2024punica | multi-tenant serving | adapter and tenant-specific serving state | adapter-aware batching, sharing, and tenant isolation | tenant interference and state multiplexing | throughput + isolation | joint phase and tenant governance remains weak | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| li2023alpaserve | model-parallel serving | partitioned model state and multiplexing metadata | statistical multiplexing and model-partition placement | interference across tenants and partitions | throughput/cost frontier | lifecycle coordination across KV and adapter state | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |
| sheng2023slora | multi-tenant adapter serving | heterogeneous LoRA adapter weights, KV cache tensors, and unified GPU memory pool | unified paging for adapters and KV cache, heterogeneous batching, and adaptive tensor parallelism | adapter fetch overhead, memory fragmentation, and batch composition jointly reshape throughput under many concurrent tenants | throughput + adapter concurrency under bounded GPU memory | does not fully address cross-model residency, eviction, or cost-to-SLO optimization across distinct base models | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| wu2024dlora | multi-tenant adapter serving | LoRA adapter weights, per-request KV cache, and replica-local adapter residency | iteration-level merged/unmerged switching, request-adapter co-placement, and selective cross-replica migration | adapter residency constrains feasible batching and cross-replica load balancing under skewed arrivals | throughput + latency + fairness under many-adapter workloads | weak adapter-version update semantics and unclear large-cluster scaling of the online optimizer | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| iliakopoulou2024chameleon | many-adapter LLM serving | GPU-resident adapter cache entries, queue state, and request size classes | adapter-cache eviction/admission, WRS-based multi-queue scheduling, and opportunistic bypass | GPU memory competition between adapter cache and request working set reshapes PCIe traffic, queueing, and tail latency | TTFT tail latency + throughput under many-adapter traces | cache policy coefficients are profile-sensitive and cross-node cache governance is missing | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| li2025toppings | adapter serving / heterogeneous execution | adapter rank matrices, CPU-side adapter computation state, and per-instance cached adapter locality | CPU-assisted LoRA execution, rank-aware routing, and instance selection by predicted delay | adapter rank determines whether locality or offloaded compute dominates decode-time interference and SLO risk | throughput + decode latency/SLO under thousands of adapters | fallback semantics under CPU saturation and broader multi-model coordination remain weak | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| zhang2025jenga | heterogeneous LLM serving | heterogeneous KV-cache allocations, fragmented GPU memory pools, and layer-specific token dependency state | two-level memory allocation, heterogeneity-aware pool organization, and eviction policy for layer-specific cache state | architectural heterogeneity creates fragmentation that limits feasible batching and reuse unless allocation respects layer-level dependencies | GPU memory utilization + serving throughput under heterogeneous model architectures | remains weaker on multi-model placement, eviction/reactivation, and cluster-level cost/SLO governance | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| yu2025prism | multi-model LLM serving | cross-model GPU KV memory, model residency/eviction state, engine-pool activation state, and shared request queue | dynamic cross-model memory remapping, model eviction/reactivation, GPU placement, and SLO-aware local scheduling | long-tail demand and bursty arrivals couple idle memory, activation delay, and deadline-miss risk across colocated models | TTFT/TPOT SLO attainment + cost-to-SLO frontier under fixed GPU budget | still weakly integrated with phase-disaggregated serving and explicit cluster-wide ownership semantics across model/KV state | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| xiang2025aegaeon | concurrent multi-model LLM serving | per-model weights, heterogeneous KV-cache state, and token-level execution progress across pooled GPUs | token-level preemptive autoscaling, prefill/decode partitioned scheduling, component reuse, and fine-grained KV-cache synchronization | GPU pooling only pays when model-switch overhead and KV-cache movement can be hidden inside per-token slack without violating serving SLOs | goodput + SLO attainment + GPU-count reduction under production multi-model serving | strongest gains rely on intra-node pooling and relaxed token slack, leaving strict low-latency or cross-node pooling regimes less resolved | LLM Serving and Short-Lived Memory Lifecycles | xiang2025aegaeon | note-ready |
| fang2026planetserve | decentralized LLM serving overlay | distributed KV-cache metadata, overlay-route state, session affinity, and node-reputation state across peer model nodes | HR-tree-based cache-aware routing, decentralized load-aware forwarding, anonymous prompt transport, and token-level quality verification | without a central scheduler, cache reuse and latency depend on whether routing metadata, session stickiness, and trust state can be synchronized cheaply enough across volatile peers | TTFT + average/P99 latency + cache-hit rate under decentralized serving workloads | geographic latency, hash-collision risk, and stronger incentive/failure semantics remain unresolved | LLM Serving and Short-Lived Memory Lifecycles | fang2026planetserve | note-ready |
| lin2024parrot | application-centric LLM serving | semantic variables, request DAGs, prompt-structure metadata, and session-scoped reusable contexts | inter-request dataflow analysis, objective deduction, prefix-aware co-location, and application-centric scheduling | multi-call workflows couple dependency latency, shared-prefix reuse, and stage-specific performance objectives across requests | end-to-end application latency + throughput | remains weak on dynamic control flow, tool/native-function integration, and richer cluster governance under heterogeneous application mixes | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| hu2025deepserve | serverless LLM serving | job/task metadata, model and KV-cache residency, and task-executor lifecycle state | request-job-task decomposition, PD-aware scheduling, locality/load-aware placement, and warm-state replication control | phase choice, cache locality, and hot-state replication jointly reshape cold-start delay, queueing, and serving throughput | production throughput + job completion time/latency + scale-up delay on Ascend NPU clusters | evidence is hardware-specific and leaves cross-tenant cache semantics and transferable governance weak | LLM Serving and Short-Lived Memory Lifecycles | TBD | context-candidate |
| miao2024specinfer | speculative LLM serving | tree-structured speculative token state, branch-local KV access state, and verification progress | token-tree construction, tree-parallel verification, and multi-step speculative sampling control | branch diversity and verification depth reshape decode-step count, KV access pattern, and serving throughput | distributed and offloaded serving speedup + verification success rate | branch-state growth and KV management under broader multi-tenant serving remain weak | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| pan2025fasttree | tree-structured LLM inference | tree-organized shared-prefix KV-cache state and context-query grouping state | tree-aware query grouping, shared-context attention kernel control, and runtime group partitioning | shared-prefix structure determines memory reuse, tensor-core utilization, and multi-query attention cost | attention-kernel speedup + end-to-end serving throughput under shared-prefix workloads | venue metadata remains unstable and gains depend on significant prefix sharing structure | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| gim2024promptcache | LLM serving | precomputed modular KV-cache segments for reusable prompt components | scheme-level prompt modularization, cache admission/eviction, and runtime KV composition | prompt overlap, prefill redundancy, and GPU memory competition jointly determine whether reuse pays off | prefill latency + end-to-end latency + throughput under reusable prompt structure | weak distributed ownership semantics, automatic module discovery, and cache invalidation after model updates | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| pan2025marconi | hybrid LLM serving | heterogeneous prefix state spanning Transformer KV cache blocks and SSM hidden state checkpoints | speculative cache admission and FLOP-aware eviction over mixed prefix state | hybrid models break token-block reuse assumptions because SSM state is all-or-nothing while KV state is partially reusable | end-to-end latency + cache efficiency under hybrid-LLM serving workloads | weak portability beyond profiled models and limited distributed coordination for heterogeneous state placement | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| yu2025pensieve | stateful LLM serving | session-scoped conversational KV-cache chunks across GPU and CPU tiers | chunk-grained eviction, ahead-of-time swap scheduling, and partial-cache recomputation control | multi-turn reuse only pays when tier placement, swap timing, and recomputation cost stay aligned with user think-time gaps | throughput + TTFT/latency under multi-turn conversation traces | weak multi-replica consistency, fragmented cache handling, and robustness to bursty think-time variation | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| gao2025cachedattention | multi-turn LLM serving | cross-round KV cache distributed across GPU HBM, host memory, and SSD | scheduler-aware prefetch/eviction, tiered AttentionStore placement, and history truncation via decoupled positional encoding | multi-turn serving couples cache reuse value, transfer latency, and scheduling foresight across storage tiers | TTFT + prefill throughput + cost under real conversation traces | depends on scheduler-visible session information and model-specific positional-encoding changes, with limited cross-node analysis | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| yu2025iccache | in-context KV reuse | chunked cross-request KV-cache segments and cache-index metadata for repeated prompt contexts | chunk-level hash indexing, longest-match cache lookup, multi-tier cache management, and RAG-oriented injected-cache precomputation | reuse only pays when matching overhead, cache residency, and prompt-sharing structure align across multi-turn and RAG workloads | TTFT + prefill throughput + GPU memory overhead under multi-turn, RAG, and code-completion traces | weak distributed cache coherence, model-update invalidation, and incomplete evidence on low-hit-rate workloads | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| wang2025kvcachewild | KV-cache workload characterization | reusable KV-cache blocks, reuse-time profiles, and request-category locality/lifespan state | workload-aware eviction and capacity sizing based on reuse time, lifespan, and spatial locality | real reuse skew, request class, and short cache lifetime determine whether residency improves hit rate and TTFT at production scale | cache hit rate + response time under real cloud traces | limited to eviction/local characterization, leaving global scheduling, tiering, and cross-node ownership weak | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| yao2025cacheblend | RAG-aware KV reuse | segment-level KV caches for retrieved text blocks plus HKVD token sets and fusion metadata | selective per-layer token recomputation and cache-fusion ratio control | missing cross-block attention makes reuse quality depend on where partial recomputation restores semantics versus added TTFT | TTFT + throughput + QA/summarization quality in RAG serving | assumes known block boundaries and limited context/storage regimes, with weak multi-tenant/shared-cache governance | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| liu2026droidspeak | cross-model KV reuse | layerwise KV caches plus transferred E-cache across fine-tuned model variants | offline critical-layer profiling, selective layer recomputation, and pipelined cache transfer | shared-prefix latency depends on which layers are semantically portable across models and whether transfer overlaps recomputation | prefill latency + throughput + quality across model pairs and workloads | profiling cost, same-architecture constraint, and weak generalized sharing beyond pairwise variants | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| cacheslide2025 | agent-oriented KV reuse | chunk-organized KV-cache blocks with cross-position-aware positional metadata | RPDC-aware cache lookup, chunked contextual position encoding, weighted correction attention, and layer-aware cache management | agent prompts preserve relative order but shift absolute position, so reuse quality depends on controlling positional drift rather than only exact prefix overlap | end-to-end latency + throughput on agent-style serving workloads | depends on agent-style prompt regularity, model-side positional adaptations, and unresolved reuse semantics beyond structured prompt workflows | LLM Serving and Short-Lived Memory Lifecycles | cacheslide2025 | note-ready |
| prabhu2025vattention | KV-cache memory management | virtually contiguous KV-cache address ranges backed by on-demand physical GPU pages | CUDA VMM-based virtual reservation, physical page commit, and address-space reuse control | serving throughput depends on eliminating software page-table indirection without giving up elastic KV-cache growth | end-to-end serving throughput + decode/prefill latency under bounded GPU memory | tied to modern CUDA VMM support and weaker on multi-node or open-ended sequence growth semantics | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| gao2025hcache | restoration-aware LLM serving | persisted hidden states and reconstructed KV cache for evicted conversational context | bubble-free layer partitioning, hidden-state persistence, and compute/I/O co-scheduled restoration control | cache-miss recovery depends on balancing storage bandwidth with partial recomputation cost instead of choosing one extreme | TTFT + storage footprint + restoration throughput under long-context and multi-turn serving | limited multi-node validation and tighter dependence on model structure, storage layout, and offline profiling | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| infinigen2024 | dynamic KV-cache eviction | per-token KV-cache entries, accumulated attention-based importance scores, and GPU/CPU-resident cache-tier state | decode-time importance tracking, low-importance KV eviction, and hierarchical cache retention control | long-context serving only stays memory-feasible when decode-time relevance rather than fixed position heuristics decides which KV entries remain GPU-resident | memory reduction + perplexity/accuracy retention + throughput under long-context inference | source note still lacks stable authorship/venue metadata and leaves prefill-side compression plus multi-node coordination open | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| qin2025mooncake | distributed KV-cache serving | globally pooled KV-cache blocks across CPU memory, SSD, and RDMA-connected serving nodes | KV-cache-aware global scheduling, remote prefix-cache reuse, and prefill/decode disaggregation control | remote reuse only pays when cache locality, transfer bandwidth, and phase placement are jointly managed across the cluster | goodput + TTFT/TBT + production request capacity under KV-cache-centric serving | benefits depend on high prefix locality and strong network/storage support, with weaker formal scheduling guarantees | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| du2025prefillonly | prefill-only LLM serving | final-layer KV cache, prefix-cache residency, and deterministic single-token completion state | suffix-KV discarding, hybrid prefilling, and SRJF-style JCT-aware scheduling for single-token workloads | discriminative LLM workloads only benefit when engines stop treating all-layer KV retention as mandatory and exploit deterministic completion time in scheduling | QPS-latency tradeoff + maximum input length + P99 latency under prefill-only serving traces | gains depend on single-token outputs and limited KV reuse, leaving mixed workloads and broader cache persistence policies open | LLM Serving and Short-Lived Memory Lifecycles | du2025prefillonly | note-ready |
| xiong2024layerkv | layer-wise KV-cache management | per-layer KV-cache blocks, residency metadata, and GPU/CPU offload state | layer-wise allocation, activity-aware offload, and SLO-aware scheduling control | TTFT becomes a queueing and memory-allocation problem once all layers need not remain resident together | TTFT + P99 TTFT + SLO violation rate under long-context serving | cross-node sharing, adaptive layer-activity prediction, and MoE-specific policies remain unresolved | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| wu2026fastserve | preemptive LLM serving | per-request KV cache, preempted execution context, and queue state across GPU/host memory | iteration-level preemption, skip-join MLFQ scheduling, and proactive KV-cache offload/restore control | latency and goodput depend on whether head-of-line blocking can be broken without making state restoration dominate execution | mean/tail latency + SLO-constrained goodput under mixed-length inference traces | fairness, multi-node migration, and coupling with disaggregated prefill/decode serving remain open | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| zhang2026jitserve | SLO-aware LLM serving | per-request SLO state, predicted output-length upper bound, dependency-pattern state, and grouped batch composition | SLO-aware admission, just-enough bandwidth allocation, and input-length-aware batch formation | prediction uncertainty and phase progress reshape goodput through queueing and batch-efficiency coupling | service goodput under mixed latency-sensitive, deadline-sensitive, and compound-request SLOs | weak coupling to explicit KV-cache ownership, cross-node transfer, and tenant-level lifecycle governance | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| sheng2024fairnessserving | fair multi-tenant LLM serving | per-client virtual token counters and queued/running request ownership state | token-level fair request selection and counter-lift admission discipline | multi-tenant arrival skew propagates through continuous batching into unequal token service unless counter state governs admission | fairness with work-conserving throughput under multi-tenant serving traces | weak integration with preemption, memory-aware fairness, and disaggregated prefill/decode serving | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| sun2024llumnix | elastic LLM serving | migratable per-request KV cache, per-instance virtual usage, and cluster-wide load state | live KV-cache migration, dynamic dispatch, load balancing, priority isolation, and autoscaling | state immobility otherwise amplifies load imbalance, fragmentation, and preemption loss across instances | median/P99 latency + preemption loss + fragmentation + autoscaling cost | decode-only migration assumption and weak treatment of tensor-parallel or cross-cluster ownership semantics | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| liu2025mell | multi-GPU KV-cache management | per-request KV-cache footprints, GPU-local request placement state, and migration-mode choices across serving GPUs | online request classification, bounded-competitive packing, and adaptive token-vs-KV migration control | multi-GPU serving wastes capacity when growing KV-cache state becomes unevenly distributed, so request migration and placement must co-govern fragmentation, utilization, and migration cost | GPU count + GPU memory utilization + migration frequency under multi-GPU serving workloads | depends on epoch-level global coordination, homogeneous GPUs, and manageable migration overhead rather than stronger decentralized or heterogeneous-cluster control | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| gao2025weaver | multi-LLM serving with attention offloading | offloaded attention tasks, split KV-cache placement across hot/cold model instances, and shared GPU-resident QKV exchange state | attention offloading, GPU-driven dynamic control flow, and queueing-aware operator splitting | hot-model throughput only improves when cold-model slack and offloaded attention latency are co-governed tightly enough to avoid head-of-line blocking on shared GPUs | TPOT + throughput + GPU memory utilization under hot/cold multi-model serving traces | only attention is offloadable and benefits depend on spare cold-model capacity plus efficient same-node GPU communication | LLM Serving and Short-Lived Memory Lifecycles | gao2025weaver | note-ready |
| ruan2026libra | phase-balanced LLM serving | split-position execution progress, micro-request KV ownership, per-GPU load state, and batch-latency profile state | flexible request partitioning, SLO-aware local batching, and chunk-based KV transfer across unified GPU pools | phase imbalance and static prefill/decode partitioning waste capacity unless split position and transfer path are co-governed | goodput and serving capacity under TBT SLOs on dynamic workloads | depends on output-length prediction and centralized scheduling, with weak evidence on larger heterogeneous clusters | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| agarwal2026symphony | disaggregated multi-turn LLM serving | session KV cache distributed across GPU memory, host memory, disk, and remote nodes | advisory-request-triggered prefetch, priority KV caching, cooperative memory management, and layerwise asynchronous restore | session stickiness, remote placement, and prefetch timeliness couple load balance to TTFT/TPOT under multi-turn serving | TTFT + TPOT + concurrency/capacity under realistic multi-turn workloads | depends on application-visible advisory signals and leaves failure/consistency semantics underdeveloped | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| lou2026hydraserve | serverless LLM serving | model weights, container/runtime initialization state, CUDA context, and cold-start worker residency state | pipeline-parallel weight fetch, stage-overlap startup orchestration, consolidation, and contention-aware placement | model-fetch bandwidth and startup critical path couple cold-start delay to SLO attainment and later steady-state performance | cold-start latency + SLO attainment + steady TPOT in public-cloud serving | weak coupling to post-start KV-cache lifecycle and limited evidence beyond bandwidth-bound cloud cold starts | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| fu2024serverlessllm | serverless LLM serving | model checkpoints across DRAM/NVMe/remote tiers plus migratable token-level inference state | load-optimized checkpoint format, tiered loading pipeline, startup-time-aware scheduling, and token-only live migration | checkpoint residency and migration choice couple startup time, queueing, and placement locality across the cluster | cold-start latency + end-to-end inference latency under serverless traces | checkpoint-centric control leaves KV ownership, multi-tenant isolation, and larger-scale fault semantics weak | LLM Serving and Short-Lived Memory Lifecycles | TBD | note-ready |
| gim2025pie | programmable LLM serving | application-visible KvPage state, embedding buffers, inferlet-local virtual resource state, and inter-inferlet shared cache handles | inferlet-programmed embed/forward/sample control, explicit KV-page allocation/copy/mask/share, and batching over command queues | emerging workflows only avoid reprefill and rigid decode loops when applications can directly govern KV reuse and generation control without breaking serving efficiency | end-to-end latency + throughput on programmable, agentic, and graph-structured serving workloads | API complexity, sandbox overhead, and multi-GPU/distributed control semantics remain open | LLM Serving and Short-Lived Memory Lifecycles | gim2025pie | note-ready |
| zheng2023sglang | LLM serving | reusable prefix KV and decoding state | radix reuse, constrained decoding, and program-structure control | program structure and reuse boundary | throughput under complex programs | cluster governance under mixed program shapes | LLM Serving and Short-Lived Memory Lifecycles | TBD | integrated |

### Comparative synthesis cluster reused in Section 3.2.1

The stream/dataflow rows below are re-integrated comparatively in Section 3.2.1 around three mechanism families: visibility and progress semantics, topology- and conflict-aware access control, and ownership transfer plus recovery coupling.

| abadi2003aurora | stream processing | operator-local continuous-query state | operator graph management and scheduling | adaptation decisions reshape downstream buffering and latency | throughput + latency in continuous queries | migration and recovery control remain implicit | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| abadi2005borealis | distributed stream processing | distributed operator state and revision metadata | adaptive operator placement and revision control | distributed adaptation changes state movement and consistency | adaptive latency/throughput under changing conditions | progress and ownership contracts remain weak | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| zhang2017revisiting | multicore stream processing | shared queues, synchronization state, and operator-local access paths | access-path exposure and synchronization design | hidden contention reshapes scalability and throughput stability | multicore scalability and throughput | observability remains weak without explicit runtime metrics | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| zhang2017mqo | complex event processing | shared subplans and intermediate event state | multi-query sharing and execution-path reorganization | sharing can amplify interference when shared state is poorly scheduled | throughput and resource efficiency | limited runtime control over shared-state interference | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| akidau2013millwheel | fault-tolerant streaming | keyed windows, timers, and persisted update state | event-time and recovery-aware execution control | record time, persistence, and replay jointly determine visible state | correctness + low-latency processing | fine-grained migration policies remain limited | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| murray2013naiad | timely dataflow | partially ordered progress state and iterative dataflow state | timely progress tracking and frontier-aware scheduling | progress tracking controls when iterative state can safely advance | low latency + iterative correctness | ownership transfer and elasticity are underdeveloped | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| akidau2015dataflow | data-parallel streaming model | windowed state plus watermark/trigger metadata | watermark, trigger, and accumulation control | output visibility depends on explicit time-progress contracts | correctness-latency-flexibility tradeoff | limited guidance for runtime reconfiguration under drift | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| fernandez2013osm | stream elasticity | operator state shards | unified scale-out/fault-tolerance transfer | migration and recovery interaction | elasticity + fault handling cost | stronger online observability | State Access and Scheduling / Runtime Control | TBD | integrated |
| carbone2015flink | stream runtime | operator state plus checkpoint/progress metadata | unified stream-batch state progression and runtime ownership | state progression interacts with elasticity and fault handling | operability + throughput + fault tolerance | finer-grained access observability remains weak | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| chandramouli2014trill | incremental stream processing | incremental operator state and temporal progress metadata | incremental maintenance and temporal advancement control | progress semantics bound which state transitions are safe | latency and high-rate analytics performance | weaker portability to elastic distributed settings | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| murray2013differential | dataflow runtime | differences and frontier-tracked shared state | progress-frontier control and incremental recomputation | safe advancement depends on explicit progress metadata | incremental update latency and correctness | access-locality and hardware effects remain implicit | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| gulisano2012streamcloud | elastic streaming | movable operator state and routing metadata | elasticity and state ownership transfer | scale-out correctness depends on controlled state movement | elasticity cost and throughput | migration coordination still coarse-grained | State Access and Scheduling / From Invisible Contention to Observable State Access | TBD | integrated |
| zhang2019briskstream | multicore stream processing | shared hotspots and NUMA-local operator state | topology-aware scheduling and placement | NUMA distance and skew reshape access cost | throughput under skew and locality pressure | recovery boundary remains implicit | State Access and Scheduling / Cost Modeling Under Locality and Topology | TBD | integrated |
| zhang2020concurrent | concurrent stateful streaming | shared state partitions and synchronization metadata | synchronization design with task assignment | lock granularity and partitioning jointly shape contention | throughput and scalability | migration and failure coupling remain weak | State Access and Scheduling / Cost Modeling Under Locality and Topology | TBD | integrated |
| zhang2023openmldb | stateful streaming joins | shared interval indexes and ordering state | index-aware concurrency control | ordering constraints couple access path and synchronization | join latency and throughput | ownership transfer policies remain limited | State Access and Scheduling / Cost Modeling Under Locality and Topology | TBD | integrated |
| mao2023morphstream | transactional stream processing | transactional dependency state and scheduling metadata | dependency-aware adaptive scheduling | dependency shape couples throughput, latency, and recovery pressure | latency and throughput stability | reuse across migration and recovery remains limited | State Access and Scheduling / Runtime Control | TBD | integrated |
| mahgoub2021sonic | chained serverless applications | inter-function data objects, dependency metadata, and handoff buffers | application-aware data passing and dependency-guided placement | client-mediated handoff and dependency serialization inflate end-to-end latency unless dependency-local transfer is runtime-managed | end-to-end DAG latency + communication overhead | reuse and scheduling semantics remain weak once heterogeneous applications share resources and stages | State Access and Scheduling / Runtime Control | TBD | note-ready |
| mahgoub2022orion | serverless DAG execution | DAG stage bundles, warm execution state, and function-level sizing metadata | DAG-aware sizing, bundling, and prewarming control | startup overhead, stage grouping, and resource sizing jointly determine DAG completion time under bursty arrivals | DAG completion latency + cost efficiency | reusable intermediate-state and multi-tenant governance remain under-specified for dynamic workflows | State Access and Scheduling / Runtime Control | TBD | note-ready |
| zhao2025rtsfaas | stateful serverless workflows | lease-grouped shared workflow state plus ownership metadata | affinity-aware lease placement, transactional scheduling, and recovery reassignment | workflow-stage locality and lease ownership jointly determine RDMA access path, cross-group coordination, and recovery cost | throughput + tail latency + recovery time under RDMA transactional workflows | centralized lease management, cross-group coordination overhead, and weak failure semantics beyond one datacenter | State Access and Scheduling / Runtime Control | TBD | note-ready |
| zhang2021caerus | serverless analytics scheduling | task-level dependency state, step graphs, and runtime produce-consume progress estimates | dependency-aware task start-time control and overlap scheduling | upstream production and downstream consumption rates couple pipeline overlap to job completion time and billable runtime | job completion time + total execution cost under serverless DAG analytics | depends on accurate rate estimates, with weak multi-job interference handling and limited explicit ownership semantics | State Access and Scheduling / Runtime Control | TBD | note-ready |
| fu2022sfs | serverless function scheduling | function-stage hints, per-core run-queue state, and cache-affinity-sensitive execution state for short-lived serverless invocations | OS-level function-aware scheduling, cross-layer hint passing, and locality-aware core assignment | short-lived functions only meet tail-latency targets when scheduler decisions preserve cache warmth and avoid generic time-slice interference during cold-start and steady execution | p99 latency + throughput + cold-start overhead under serverless workloads | depends on runtime-provided function hints and leaves multi-tenant fairness, security, and heterogeneous accelerator coordination open | State Access and Scheduling / Runtime Control | fu2022sfs | note-ready |
| hong2025sola | SLO-aware LLM serving | per-request TTFT/TPOT state, predicted remaining length, queue state, and KV-memory pressure | iteration-level priority switching and state-aware objective control over request and token scheduling | phase asymmetry and latency-state imbalance couple batching and admission to goodput and SLO attainment | SLO attainment + goodput under TTFT/TPOT constraints | weak support for disaggregated prefill/decode and limited cluster-wide ownership/admission semantics | State Access and Scheduling / Runtime Control | TBD | note-ready |
| oh2024exegpt | constraint-aware LLM inference scheduling | sequence-length distribution, offline execution-time profiles, and batch/parallelism configuration state | profiling-driven search over batch size, decode microbatching, tensor parallelism, and layer allocation | prefill/decode asymmetry and length skew couple configuration choice to latency-constrained throughput | throughput under latency constraints | ignores explicit KV capacity/eviction and has weak online adaptation under workload drift | State Access and Scheduling / Runtime Control | TBD | note-ready |
| zhao2024alise | speculative LLM serving scheduling | predicted request lengths, per-request remaining execution state, and swappable KV-cache residency across GPU and CPU memory | retrieval-based length prediction, iteration-level preemptive multi-queue scheduling, and EWT-guided KV-cache swap/restore control | heterogeneous request lengths only stop causing head-of-line blocking when speculative execution-time estimates are accurate enough to justify preemption and KV-cache movement | normalized latency + throughput under heterogeneous online-serving traces | prediction cold-start, single-node scope, and weaker robustness under distribution shift or extreme memory pressure remain open | State Access and Scheduling / Runtime Control | zhao2024alise | note-ready |
| he2025resourcemultiplexing | joint tuning-and-serving runtime multiplexing | decode-step execution state, suspendable PEFT forward/backward tasklets, shared base-model residency, and LoRA training state on one GPU | SLO-aware iteration-level budget planning, coroutine-based suspend/resume autograd, and decode-plus-PEFT fused execution | mixed tuning and serving only coexist when memory-bound decode slack can be converted into bounded PEFT work without violating TTFT/TPOT targets | PEFT throughput + P99 TTFT/TPOT under concurrent serving-and-tuning workloads | multi-GPU synchronization overhead, lack of pipeline-parallel support, and tighter whole-cluster integration remain open | State Access and Scheduling / Runtime Control | he2025resourcemultiplexing | note-ready |
| wang2025sirius | inference-training GPU memory handover | shared GPU memory-pool ownership, training-stage state, idle-model residency, and reclaimable inference/training memory state | instant training-memory adjustment, explicit ownership transfer, and SLO-aware reallocation control for colocated inference and training | colocation only preserves inference SLOs when training-stage structure makes GPU memory reclaim fast enough and ownership transfer avoids allocator and cache-induced delay | SLO attainment + P99 latency + training throughput under colocated inference/training workloads | currently limited to data parallel training and simplified queueing assumptions, with weak support for richer model heterogeneity and model-parallel training | State Access and Scheduling / Runtime Control | wang2025sirius | note-ready |
| nasir2019megaphone | stream migration | keyed operator state | fine-grained state migration | migration latency and locality | latency under reconfiguration | broader fault coupling | State Access and Scheduling / Runtime Control | TBD | integrated |
| mao2025spacker | stream migration | movable distributed state shards | unified redistribution substrate | transfer reuse across scaling and placement | migration overhead + stability | explicit ownership semantics | State Access and Scheduling / Runtime Control | TBD | integrated |
| zhao2024recovery | transactional stream processing | recovery metadata and replay state | recovery-integrated scheduling | replay pressure and steady-state interference | recovery time + steady-state overhead | unified ownership and migration control | State Access and Scheduling / Runtime Control | TBD | integrated |
| carbone2015asynchronous | stream checkpointing | checkpoint snapshots and in-flight state | asynchronous barrier-aligned checkpointing | checkpoint delay and recovery coupling | runtime overhead + fault tolerance | explicit ownership-transfer composition | State Access and Scheduling / Runtime Control | TBD | integrated |
| wan2025bytecheckpoint | distributed training checkpointing | model weights, optimizer shards, scheduler state, and reshardable checkpoint metadata | parallelism-agnostic checkpoint encoding, save/load orchestration, and reshard control | training elasticity, recovery, and framework/storage heterogeneity all depend on whether checkpoint state can move across parallel configurations without offline conversion | save/load time + training stall time + I/O throughput in production-scale model training | incremental checkpoints, cross-region transfer, and stronger framework-independent ownership semantics remain weak | State Access and Scheduling / Runtime Control | wan2025bytecheckpoint | note-ready |
| huang2025flowcheck | distributed training checkpointing | global gradients, model-update state, and mirrored checkpoint replicas | switch-level traffic mirroring, packet parsing, and asynchronous checkpoint reconstruction | allreduce traffic already carries recoverable state, so checkpoint cost shifts from GPU stall to network mirroring and packet loss handling | effective training time + checkpoint overhead under failures | tied to allreduce-visible training semantics and dedicated checkpoint nodes | State Access and Scheduling / Runtime Control | TBD | note-ready |
| lian2025universal | distributed training checkpointing | reshardable model weights, optimizer shards, and training-progress metadata | parallelism-agnostic atomic checkpointing and pattern-based reshard control | restart and elasticity depend on whether checkpoint state can be reconstructed across DP, TP, PP, SP, and ZeRO layouts | reconfiguration time + restart continuity at scale | storage amplification and framework coupling remain high | State Access and Scheduling / Runtime Control | TBD | note-ready |
| gao2025deck | delta checkpointing for recommendation training | sparse embedding deltas, row-index traces, and tiered checkpoint chunks | delta tracking, hierarchical merge, and multi-tier staging control | sparse row updates couple checkpoint frequency to staging bandwidth and recovery lineage depth | effective training time + checkpoint frequency + recovery overhead | sparse-update assumptions and merge-policy stability limit portability beyond recommendation training | State Access and Scheduling / Runtime Control | TBD | note-ready |
| cai2025moc | MoE training fault tolerance | expert weights, optimizer shards, and two-level checkpoint replicas | partial-expert checkpointing, sharded persistence, and two-level snapshot/persist control | sparse expert activation couples checkpoint cost to which experts are preserved and how loss accumulates under failures | checkpoint overhead + training throughput + recovery quality | PLT thresholds and expert-selection policy remain MoE-specific and weakly generalized | State Access and Scheduling / Runtime Control | TBD | note-ready |
| wei2025phoenixos | GPU process checkpoint/restore | GPU buffers, execution context, and recoverable process state | validated-speculation dirty tracking plus concurrent checkpoint/restore control | migration and fault recovery depend on tracing GPU write sets without stop-the-world stalls | downtime + migration latency + cold-start latency + training interruption | GPU-memory overhead and speculation portability across kernels and hardware remain open | State Access and Scheduling / Runtime Control | TBD | note-ready |
| mohan2021checkfreq | DNN training checkpointing | model weights, optimizer state, and recoverable data-iterator progress | two-stage snapshot/persist pipelining and adaptive checkpoint-frequency control | checkpoint value depends on how snapshot latency, persist latency, and iteration structure overlap under accelerator memory limits | recovery time + training overhead under frequent failures or preemption | limited support beyond data parallelism and weaker applicability when spare GPU memory disappears at large scale | State Access and Scheduling / Runtime Control | TBD | note-ready |
| fu2024autocheck | checkpoint-state identification | dynamically traced program variables, reduced dependency graphs, and loop-carried recovery state | trace-driven dependency analysis, DDG contraction, and checkpoint-variable identification control | recovery correctness depends on whether loop-carried state is isolated precisely enough to avoid both missing and redundant checkpoint variables | checkpoint correctness + analysis time + checkpoint-size reduction versus full-process checkpointing | depends on dynamic traces and manual loop selection, with weak support for richer distributed runtime state and online integration | State Access and Scheduling / Runtime Control | fu2024autocheck | note-ready |
| eisenman2022checknrun | sparse-model checkpointing | embedding-table rows, dense model weights, optimizer state, and reader progress in recommendation-model training | differential checkpoint selection, intermittent full-vs-incremental switching, and quantized persistence control | checkpoint cost depends on sparse row updates, snapshot decoupling, and the tradeoff between write bandwidth and cumulative restore error | write bandwidth + storage footprint + recovery accuracy under production recommendation training | cumulative differential growth, weaker treatment of non-embedding state, and limited guarantees under repeated restore cycles | State Access and Scheduling / Runtime Control | TBD | note-ready |
| lin2025weipipe | long-context large-model training parallelism | pipeline-transferred weights and gradients plus worker-local full-sequence activations | weight pipeline parallelism, forward-backward interleaving, and ring-based weight/gradient circulation control | when sequence length and microbatch size make activations dominate communication, switching the transferred state object from activations to weights reshapes throughput, bubble cost, and memory pressure | training throughput + peak memory under long-context distributed training | openreview-only metadata, incomplete TP/FSDP composition story, and weak dynamic switching when short-context regimes favor classic activation passing | State Access and Scheduling / Runtime Control | TBD | note-ready |
| liu2025mario | activation checkpointing in pipeline parallelism | checkpointed activations, recomputation schedule state, and pipeline-bubble execution windows | graph-level checkpoint placement, bubble-overlapped recomputation, redundancy elimination, and forward preposing control | checkpointing overhead only disappears when recomputation is moved off the critical path into pipeline bubbles without recreating memory imbalance | throughput retention + activation-memory reduction under pipeline-parallel LLM training | gains depend on sufficiently large and predictable pipeline bubbles and remain weaker when small models or small microbatches leave little overlap space | State Access and Scheduling / Runtime Control | liu2025mario | note-ready |
| arfeen2024pipefill | pipeline-parallel LLM training utilization | predictable pipeline-bubble windows, fill-task execution state, and preemptible low-priority GPU kernels | bubble-window scheduling, low-priority fill-task dispatch, and fast preemption/isolation control | GPU utilization only rises safely when deterministic bubble windows can host auxiliary kernels without perturbing the timing of the main pipeline | training throughput preservation + fill-task throughput + aggregate GPU utilization under pipeline-parallel training | assumes stable bubble structure and lightweight preemptible fill tasks, leaving large-cluster coordination, heavy data-motion tasks, and richer multi-tenant isolation open | State Access and Scheduling / Runtime Control | arfeen2024pipefill | note-ready |
| feng2025optimus | multimodal LLM training bubble scheduling | per-GPU bubble windows, colocated encoder/LLM model state, and microbatch dependency state across separate encoder and LLM pipelines | separate encoder/LLM parallel-plan search, kernel-level bubble filling, and two-stage local/global dependency scheduling | multimodal training only benefits from bubble exploitation when encoder work can be decomposed finely enough to fit LLM communication and pipeline slack without violating microbatch dependencies | iteration time + MFU + aggregate throughput under large-scale multimodal training | depends on stable offline profiling, constrained encoder/LLM parallel-plan compatibility, and incomplete online adaptation to runtime drift | State Access and Scheduling / Runtime Control | feng2025optimus | note-ready |
| jiang2024megascale | hyperscale LLM training runtime coordination | training progress, fault state, communication overlap state, and cluster-wide observability traces across 10k+ GPUs | PTB communication-compute overlap, fault-aware recovery workflow, and system-wide observability-driven runtime control | training efficiency at 10k+ GPUs depends on jointly controlling overlap, straggler diagnosis, and fault recovery rather than optimizing any single layer in isolation | MFU + throughput scaling + recovery time at production training scale | the design remains heavily system-specific and engineering-intensive, with limited guidance for smaller or more heterogeneous clusters | State Access and Scheduling / Runtime Control | jiang2024megascale | note-ready |
| zhang2020finestream | hardware-conscious execution | window state and device-resident buffers | topology-aware operator mapping and state-path design | CPU/GPU movement reshapes throughput and delay | throughput + latency on integrated architectures | portability across heterogeneous device mixes | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| zeng2024cstream | compressed-state execution | compression dictionaries and chunk metadata | compression-path selection and asymmetric-core scheduling | state representation changes energy, delay, and compression ratio jointly | throughput + delay + energy | weak coupling to service-level quality budgets | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| ren2020hmg | hierarchical multi-GPU cache coherence | GPU-private cachelines, directory/coherence metadata, and scope-aware memory-ordering state across hierarchical inter-GPU links | hierarchy-aware coherence protocol design, scoped memory ordering, and locality-sensitive coherence control | coherence traffic only scales across hierarchical multi-GPU fabrics when remote access patterns, on-package locality, and scope semantics are jointly respected instead of flattened into one uniform coherence domain | performance under hierarchical multi-GPU workloads + coherence overhead under NUMA-sensitive access patterns | tied to hierarchical GPU topologies and scoped memory semantics, with limited guidance for larger heterogeneous memory fabrics or runtime-managed fault handling | Hardware-Conscious and Approximation-Aware Stateful Execution | ren2020hmg | note-ready |
| zhang2023compressstreamdb | compressed query execution | compressed tuples and operator-format metadata | execution-without-decompression and operator-path control | format choice reshapes access cost and pipeline behavior | throughput + memory overhead | closed-loop switching under workload drift remains limited | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| yu2025adaptivecompression | adaptive compressed execution | data-aware encoding state | adaptive compression level and format control | compression ratio alters memory traffic and downstream latency | throughput + memory + delay | disturbance-aware switching policies remain weak | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| zhang2023g10 | GPU memory oversubscription / tensor migration | tensor residency state across GPU DRAM, host DRAM, and SSD plus lifetime metadata | compiler-guided eviction, prefetch, and tier placement | tensor lifetime prediction reshapes migration overlap, memory pressure, and stall behavior under oversubscribed training | training throughput relative to ideal infinite-memory execution | depends on predictable execution timing and remains weak on multi-GPU, online adaptation, and serving-time state lifecycles | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| maurya2025mlpoffload | multi-tier LLM training state offload | optimizer state shards, FP16 gradients, and subgroup buffers spread across GPU HBM, host DRAM, NVMe, and parallel file system tiers | bandwidth-proportional subgroup placement, cache-friendly subgroup reordering, delayed gradient upcasting, and per-medium concurrency control | optimizer-state movement only scales when transfer paths, cache reuse, and storage-medium contention are jointly governed across the hierarchy | iteration time + update throughput + effective I/O bandwidth in large-model pre-training | depends on HPC-style PFS availability and remains weak on asynchronous updates, cloud storage variability, and broader runtime portability | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| nayak2024fusemax | attention-accelerator cross-layer co-design | streaming softmax statistics, tiled QKV fragments, and epoch-local partial output state in a cascade-of-einsums execution | one-pass attention cascade selection, intra-epoch interleaving, and spatial-array binding/control of softmax-matmul fusion | attention acceleration only scales with long sequences when pass structure, on-chip storage footprint, and heterogeneous array utilization are co-optimized together | attention latency + end-to-end transformer speedup + energy on spatial accelerators | accelerator-specific assumptions and unresolved metadata normalization limit direct portability to commodity GPU runtimes | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| fu2024autoscratch | GPU cache management for inference | L2 cache lines, residency-controlled pages, and scratchpad allocation state | ML-guided scratchpad admission and capacity partitioning via L2 residency controls | kernel-level access-pattern prediction reshapes cache pollution, HBM traffic, and inference latency | L2 hit rate + end-to-end inference latency on inference-oriented GPU workloads | single-model and single-tenant assumptions, plus weak linkage to distributed or long-lived runtime state governance | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| joo2025coruscant | sparse LLM inference acceleration | bitmap-encoded unstructured sparse weights plus decompression pipeline state across registers, shared memory, and sparse tensor cores | bitmap-format selection, software/hardware decode placement, and load-decompress-compute pipelining | in memory-bound decode, compression only pays if decompression overhead and tensor-core feed path stay below the bandwidth savings | SpMM speedup + end-to-end token throughput + area/power overhead under sparse LLM inference | requires custom sparse tensor-core support and remains focused on static weight sparsity rather than dynamic KV lifecycle control | Hardware-Conscious and Approximation-Aware Stateful Execution | joo2025coruscant | note-ready |
| tian2025clone | latency-aware edge LLM customization | pruned model weights, task-conditioned LoRA adapters, and layer-wise DVFS operating state on edge devices | generative pruning-ratio search, request-wise adapter routing, and layer-level DVFS control | edge inference only meets latency and energy budgets when model compression, adapter choice, and per-layer hardware operating point are co-optimized together | latency + energy + task accuracy on edge LLM inference platforms | single-device scope, accuracy degradation from pruning, and hardware-specific deployment assumptions limit broader runtime portability | Hardware-Conscious and Approximation-Aware Stateful Execution | tian2025clone | note-ready |
| fang2025anda | activation-format-aware LLM inference acceleration | grouped activation state with shared exponents, variable-length mantissas, and bit-plane layout metadata for weight-only inference | module-wise mantissa assignment, online FP-to-Anda conversion, and bit-serial execution over bit-plane activations | activation representation governs memory traffic, arithmetic complexity, and achievable speedup once weight-only quantization shifts the bottleneck from weights to activations | speedup + area efficiency + energy efficiency + perplexity under weight-only LLM inference | requires custom accelerator support and leaves GPU deployment, KV-cache interaction, and broader serving integration unresolved | Hardware-Conscious and Approximation-Aware Stateful Execution | fang2025anda | note-ready |
| gerogiannis2025deca | compressed-weight LLM inference acceleration | compressed weight tiles, quantization metadata, and decompression pipeline state between memory and matrix engines | roof-surface-guided decompression placement, near-core hardware decompression, and TEPL-mediated overlap control | compressed CPU inference only realizes bandwidth gains when decompression throughput stops bottlenecking the path between memory and matrix compute | next-token latency + compressed GeMM speedup + hardware area under CPU LLM inference | requires ISA and hardware changes and remains focused on CPU-side compressed weights rather than broader runtime lifecycle control | Hardware-Conscious and Approximation-Aware Stateful Execution | gerogiannis2025deca | note-ready |
| liu2025hermes | heterogeneous LLM inference / near-data offload | hot/cold-partitioned MLP weights, neuron-activity history, and NDP-DIMM placements | offline ILP partitioning, online hot/cold prediction, and inter-DIMM remapping | weight placement reshapes PCIe traffic, GPU/NDP balance, and slowest-DIMM bottlenecks | tokens/s + cost efficiency on consumer-grade heterogeneous inference | attention and KV-cache lifecycle remain weak, and the design depends on specialized NDP-DIMM assumptions | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| vuppalapati2024colloid | tiered memory / latency-aware page placement | page residency across fast/slow tiers plus per-tier latency state | latency-aware waterline search and batch page migration | page placement reshapes queueing and access-latency balance across tiers | application runtime + throughput under two-tier memory | Intel-specific observability and weak multi-tier, GPU, and serving-lifecycle portability | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| liu2025beyondhotness | tiered memory / MLP-aware page placement | per-page tier residency plus AOL/MLP latency-sensitivity metadata | profile-guided placement and AOL-threshold dynamic migration | MLP-sensitive placement changes exposed stall time rather than raw hotness | end-to-end performance on NUMA/CXL-style two-tier memory | PMU dependence, profile drift, and no explicit serving-time lifecycle semantics | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| gu2025cent | CXL-enabled PIM inference | model weights, KV cache, and intermediate embeddings distributed across CXL-connected PIM devices | PP/TP mapping, CXL communication primitives, and PIM/PNM execution placement | memory-bandwidth mismatch and CXL communication overhead jointly determine feasible throughput and latency | throughput + energy + tokens per dollar under long-context inference | evidence is simulator-based, with weak online-serving dynamics and unresolved TP scaling limits | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| song2024powerinfer | consumer-grade heterogeneous LLM inference | hot/cold neuron partitions, activation-prediction state, and CPU/GPU-resident split MLP weights | neuron hotness profiling, hybrid GPU/CPU execution placement, and runtime activation-guided sparse execution | consumer-GPU serving only becomes practical when sparse neuron activity is stable enough to keep hot weights resident on GPU while offloading cold computation to CPU without excessive transfer | tokens/s + acceleration over CPU-offload baselines + quality retention on commodity GPUs | benefits depend strongly on activation sparsity and weaken for dense-gated models, high-batch serving, and broader server workloads | Hardware-Conscious and Approximation-Aware Stateful Execution | song2024powerinfer | note-ready |
| kim2025pimba | post-transformer LLM serving acceleration | state matrices for SSM/linear-attention models plus attention-side KV/cache fragments mapped into PIM-accessible memory | access-interleaved shared-SPU scheduling, MX8 state quantization, and unified PIM offload for state-update and attention operators | post-transformer serving only benefits when state-update bandwidth, bank-level read/write scheduling, and GPU-PIM coordination are co-designed rather than treating state updates like generic GEMV | token-generation throughput + state-update latency + energy/area efficiency under SU-LLM and hybrid-model serving | blocked GPU-PIM execution, hardware dependence, and incomplete evidence on larger-scale online serving remain open | Hardware-Conscious and Approximation-Aware Stateful Execution | kim2025pimba | note-ready |
| zhang2025qfactory | quantized LLM serving compilation | weight-only quantized tensors plus scale/zero-point metadata and Qtile-annotated computation graphs | deferred dequantization, Qtile-graph transformation, and differentiated scheduling of weights versus quantization metadata | low-bit serving only realizes its theoretical memory-bandwidth gains when quantization metadata reuse and dequantization placement are co-optimized at compile time | kernel latency + memory-bandwidth utilization + end-to-end serving throughput for low-bit LLMs | limited to weight-only quantization and weaker benefits at larger batch sizes or richer dynamic runtime settings | Hardware-Conscious and Approximation-Aware Stateful Execution | zhang2025qfactory | note-ready |
| chen2025heteroinfer | mobile heterogeneous LLM inference | per-operator weight/activation partitions, GPU-NPU synchronization flags, and shared UMA-resident execution buffers | offline profiling-guided tensor/layer partitioning, fast flag-based synchronization, and phase-specific GPU-NPU scheduling | heterogeneous acceleration only pays when shape-sensitive NPU kernels, GPU fallback work, and synchronization overhead are jointly controlled on UMA mobile SoCs | prefill/decode throughput + end-to-end latency + energy under mobile on-device inference | tightly coupled to Snapdragon/QNN-specific hardware behavior and leaves cross-platform adaptation and multi-request scheduling open | Hardware-Conscious and Approximation-Aware Stateful Execution | chen2025heteroinfer | note-ready |
| kim2025lia | heterogeneous CPU-GPU LLM inference | decoder sublayer parameters, activations, and KV-cache placement across CPU, GPU, DDR, and CXL tiers | sublayer CPU/GPU mapping, full-layer caching, prefill/decode overlap, and static CXL/DDR tiering control | sublayer placement determines PCIe traffic and AMX/GPU overlap, which in turn reshapes latency and throughput on single-GPU serving | latency + throughput + energy efficiency + cost on single-GPU heterogeneous serving | strongly tied to AMX and fixed decoder structure, with weak support for dynamic batching, multi-GPU execution, and finer-grained CXL management | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| mercury2025rms | remote-memory-aware multi-GPU execution | sharded operator inputs, shared inputs, and remotely addressable HBM state across GPUs | CommIR-based parallelize, shift, shard, replicate, and search-guided remote-memory scheduling | remote-memory scheduling changes copy volume, local HBM pressure, and communication overlap across multi-GPU operators | operator latency and design-space coverage under varying topologies and sequence lengths | remains compiler-centric rather than runtime-centric, and online adaptation under dynamic serving loads is still weak | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| guo2024gmlake | GPU memory defragmentation for large-model training | fragmented GPU physical blocks, virtually stitched allocation state, and allocator-visible free-block pools under training-time memory pressure | CUDA VMM-based virtual-memory stitching, pBlock/sBlock pool management, and split-vs-stitch allocation control | memory-saving techniques only translate into usable HBM capacity when fragmented free space can be reassembled fast enough to preserve training throughput and larger batch feasibility | peak GPU memory consumption + fragmentation rate + training throughput under large-model training | depends on CUDA VMM behavior and training regularity, with weaker evidence for less-structured allocation patterns or non-NVIDIA stacks | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| he2025waferllm | wafer-scale LLM inference | model weights, activation shards, and KV-cache segments distributed across hundreds of thousands of mesh-connected cores with local SRAM | PLMR-guided phase-specific partition/replication, MeshGEMM/MeshGEMV scheduling, and KV-cache shift placement | wafer-scale inference only realizes on-chip bandwidth gains when non-uniform mesh latency, local-memory limits, and reduction routes are co-optimized with state placement | GEMM/GEMV speedup + end-to-end inference throughput + energy efficiency on wafer-scale accelerators | evidence centers on one wafer-scale platform and leaves MoE support, multi-wafer scaling, and richer serving features open | Hardware-Conscious and Approximation-Aware Stateful Execution | he2025waferllm | note-ready |
| hong2024dramcache | heterogeneous GPU memory hierarchy | DRAM cachelines, tag metadata, SCM-resident main data, and per-page access statistics | SCM-aware bypass, configurable tag-cache partitioning, and metadata placement | page-level access patterns reshape DRAM probe traffic, SCM writes, and effective bandwidth under oversubscribed GPU memory | simulated performance + energy under GPU oversubscription | simulator-only evidence and weak linkage to runtime-managed model or KV lifecycles | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| lee2025tiertune | tiered memory / cache-migration co-management | LLC way partitions plus page residency across near/far memory | dynamic LLC repartitioning plus hold/resume page migration | cache partitioning reshapes per-tier latency, bandwidth balance, and migration debt | IPC + bandwidth utilization + migration count in tiered memory | simulator-only evidence, hardware modifications, and weak deployment portability | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| kang2025turboattention | approximate attention execution for high-throughput LLM inference | quantized KV-cache blocks, head-level mixed-precision assignments, and fused approximate-softmax kernel state | per-block KV quantization, head-level bitwidth allocation, and fused softmax-approximation attention kernels | KV representation and approximate softmax co-determine HBM traffic, tensor-core utilization, and attention latency under long-context decoding | end-to-end latency + throughput + quality retention under approximate attention | quality evidence is strong but still bounded to evaluated tasks and NVIDIA-oriented fused kernels | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| sanovar2024leanattention | hardware-aware decode attention execution | per-tile partial attention tuples, decode-phase query state, and long-context KV-cache tiles | lean-tile partitioning, CTA-level context parallelization, and associative reduction over tile-local softmax state | decode attention only scales when long-context KV work is decomposed into enough hardware-sized tiles to keep SMs busy without letting reduction overhead dominate | attention-kernel latency + end-to-end decode throughput under long-context inference | reduction overhead, offline tile tuning, and weaker gains in already-saturated regimes still limit portability across broader hardware and serving stacks | Hardware-Conscious and Approximation-Aware Stateful Execution | sanovar2024leanattention | note-ready |
| pan2025instattention | in-storage long-context LLM inference | decode-phase KV cache placed across GPU memory and flash-backed computational storage, plus sparse attention index state | in-storage attention offloading, Flash-aware dual-step sparse loading, and token-indexed plus hidden-embedding-indexed KV mapping | decode becomes memory-bound once KV cache outgrows GPU memory, so placing attention near flash bandwidth rather than PCIe transfer directly reshapes throughput | throughput + latency under long-context offline inference with bounded GPU memory | depends on specialized computational-storage hardware, sparse-attention quality tradeoffs, and unresolved long-run flash GC/write amplification behavior | LLM Serving and Short-Lived Memory Lifecycles | pan2025instattention | note-ready |
| liu2025heterrag | heterogeneous PIM RAG acceleration | retrieval index state, document/vector locality state, and generation-side KV/prefix state split across DIMM-PIM and HBM-PIM tiers | stage-specific placement of retrieval versus generation, locality-aware retrieval/generation, and fine-grained parallel pipeline between the two stages | retrieval needs large low-cost capacity while generation needs high memory bandwidth, so mismatched placement directly turns cross-tier transfer into the end-to-end bottleneck | throughput + latency + energy for end-to-end RAG pipelines | evidence is simulation-based, tied to a specific HNSW-style retrieval stack, and broader service-quality boundaries remain weak | Retrieval Memory and Vector-Index State Layers | TBD | note-ready |
| federici2025dip | dynamic sparse on-device LLM inference | token-conditioned activation masks, cache-aware weight-column residency state, and lightweight compensation adapters | dynamic input pruning, cache-aware masking, and LoRA-based compensation for pruned SwiGLU activations | activation magnitude and cache residency jointly determine flash/DRAM traffic, weight reuse, and token throughput on memory-bound devices | memory footprint + token throughput + perplexity/accuracy on mobile-style hardware | centered on MLP weight loading and on-device memory traffic rather than broader KV lifecycle governance, multi-tenant serving, or distributed state control | Hardware-Conscious and Approximation-Aware Stateful Execution | federici2025dip | note-ready |
| zheng2025save | fault-tolerant model inference under GPU memory bit flips | per-bit vulnerability classes for GPU-resident model and activation state plus reliable/unreliable memory placement state | offline bit criticality analysis, reliability-aware placement, asynchronous verification, and layer-level replay control | inference reliability depends on matching scarce reliable memory to the most vulnerable state while overlapping verification so protection does not dominate latency | accuracy retention under injected bit flips + end-to-end latency overhead + recovery time | depends on small reliable-memory regions, static graphs, and leaves large-LLM/KV-cache protection unresolved | Hardware-Conscious and Approximation-Aware Stateful Execution | zheng2025save | note-ready |
| cao2025moelightning | memory-constrained MoE inference | paged expert weights, CPU-resident KV cache, microbatch hidden states, and CPU/GPU/I/O pipeline progress state | hierarchical roofline-guided strategy search, microbatch CGOPipe scheduling, and paged weight management | microbatch sizing and offload placement jointly determine overlap among PCIe transfer, GPU FFN compute, and CPU attention execution | decode throughput on memory-constrained GPUs under CPU-assisted offload | gains remain tied to single-node CPU-memory offload and leave distributed expert coordination, disk-tier offload, and dynamic workload adaptation unresolved | Hardware-Conscious and Approximation-Aware Stateful Execution | cao2025moelightning | note-ready |
| yun2024duplex | Logic-PIM-based LLM inference hardware | expert weights, grouped-query attention state, and bank-bundle-partitioned memory traffic across xPU and Logic-PIM units | expert co-processing, attention co-processing, and bank-bundle memory partitioning between xPU and Logic-PIM | MoE and GQA throughput depend on matching low-Op/B layers to memory-near execution without letting cross-device coordination dominate | throughput + latency + energy under MoE/GQA-heavy LLM inference | simulator-based evidence, hardware-modification requirements, and unresolved canonical venue metadata limit immediate portability | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| zhu2024sampleattention | adaptive sparse long-context attention execution | sampled attention-score summaries, block-level column/slash stripe scores, and block-sparse KV-selection masks | query-guided chunk sampling, CRA-thresholded KV block filtering, and fused sparse-attention kernel execution | prefill speedup depends on whether sampled scores preserve enough attention mass to skip most KV blocks without accuracy loss | prefill latency + TTFT + accuracy under long-context inference | benefits weaken on short sequences and the method leaves decode-time state management and distributed execution open | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| ye2025flashinfer | customizable attention engine for LLM serving | block-sparse KV-cache state, shared-prefix attention state, and runtime tile scheduling state | block-sparse attention templating, JIT-specialized kernel generation, and load-balanced runtime dispatch | KV layout heterogeneity and runtime sequence skew jointly determine token latency and attention-engine efficiency | token latency + end-to-end serving throughput across diverse attention variants | depends on NVIDIA-centric kernel generation and remains weaker on long-horizon lifecycle governance beyond attention execution | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| zhao2024atom | low-bit LLM serving / mixed-precision execution | low-bit weights, activations, and quantized KV cache with channel-reordering metadata | outlier-aware mixed precision, fine-grained group quantization, fused GEMM/dequantization, and low-bit KV-cache control | representation choice couples tensor-core utilization, dequantization cost, and KV memory traffic to serving throughput | end-to-end serving throughput + perplexity/zero-shot accuracy under single-GPU serving | weak online adaptivity, limited lifecycle coupling to KV eviction/offload, and uncertain portability beyond tuned GPU kernels | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| park2025decdec | low-bit LLM quantization compensation | CPU-resident quantization residuals, token-conditioned salient-channel state, and zero-copy residual-fetch state across CPU/GPU memory | offline residual quantization, online salient-channel selection, and zero-copy residual compensation control | on-device low-bit serving only regains model quality when the system can fetch just the currently salient residual channels without letting CPU-GPU transfer dominate decode latency | perplexity/accuracy recovery + decode-latency overhead under low-bit on-device inference | relies on extra CPU memory, single-batch on-device settings, and hardware/connect interconnect assumptions that remain weaker for broader serving deployments | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| xia2024quantllm | FP6-centric quantized LLM inference | FP6-compressed model weights transformed into INT4 plus INT2 layouts and sparse metadata | offline format conversion, Tensor-Core-friendly kernel design, and optional 4:2 sparsity selection | nonstandard weight representation changes memory access, kernel efficiency, and feasible deployment footprint in decode-heavy inference | inference throughput + model-quality retention under modern GPU inference | largely weight-centric, with weak activation/KV integration and limited runtime control beyond offline format choice | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| fan2025spinfer | sparse GPU LLM inference | sparsified weight tensors plus bitmap-encoded nonzero metadata | tensor-core-aware bitmap encoding, shared-memory bitmap decode, and asynchronous sparse-kernel pipelining | sparse representation changes index overhead, tensor-core utilization, and end-to-end inference latency | end-to-end inference speedup + compression efficiency under low-sparsity LLM inference | pruning-quality dependence and weak linkage to KV-lifecycle or multi-tenant serving governance | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| kamath2025podattention | prefill-decode overlap execution | prefetched KV-cache transfer state, overlap scheduling state, and prefill/decode pipeline progress | overlap-aware prefill/decode scheduling and transfer-path control | full overlap only pays when transfer timing, decode consumption, and prefill completion stay aligned on the serving path | TTFT + throughput under overlapped LLM inference | weak multi-tenant governance and limited coupling to broader KV lifecycle policies | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| agarwal2013blinkdb | approximate analytics | sample and synopsis state | bounded-error/bounded-time query planning | sample choice couples latency and error envelope | response time + error bounds | online update and reuse debt under drift | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| goiri2015approxhadoop | approximate batch analytics | approximate task state and sampled input partitions | approximate execution-mode selection | skipped work changes completion time and accuracy | job time + accuracy | weak inter-stage state reuse semantics | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| wang2018approxjoin | approximate distributed joins | sampled join summaries and partition samples | join-side sampling and plan control | sample allocation affects network cost and error | latency + accuracy | dynamic skew adaptation remains weak | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| hu2018streamapprox | approximate stream analytics | online stratified samples | online sampling-rate control | sample maintenance affects throughput and accuracy drift | throughput + error | limited cross-operator composition | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| chen2018incapprox | incremental approximate analytics | reusable sampled state and partial results | incremental sample maintenance and reuse control | update cost couples with future error and reuse value | latency + error + update cost | long-horizon maintenance accounting remains weak | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| zeng2024pecj | approximation-aware stream execution | compensation metadata and join state | proactive compensation and quality-bound control | compensation cost and disorder jointly determine quality stability | accuracy + latency under disorder | recovery-time error contracts remain unspecified | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| zeng2024libamm | approximate compute kernel | sketched matrix state and kernel metadata | algorithm selection under accuracy budget | approximation interacts with memory behavior and data distribution | speed + accuracy frontier | service-level composition across pipelines remains weak | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| tang2025freesam | adaptive sampling execution | join samples and fidelity metadata | sample-size adaptation under quantity-quality goals | sample quantity affects cost and bounded quality | throughput + quality | coupling to downstream service SLOs remains weak | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| xu2024leap | approximate materialization | predicted sample views and utility metadata | predictive sample selection | sample reuse shifts maintenance cost and answer quality | latency + quality | drift-aware refresh policy remains limited | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | integrated |
| wu2023sentistream | adaptive stream learning | evolving model state and co-training update traces | co-training and online adaptation control | update policy couples concept drift response with future model quality | adaptation quality under evolving streams | maintenance and retention costs remain under-modeled | State Evolution and Reuse / From Updates to Long-Horizon Memory | TBD | integrated |
| wang2024mostream | stream clustering | evolving summaries, windows, and refinement state | modular self-optimization over clustering components | summarization and refinement control determine whether updates remain stable online | clustering quality + online efficiency | weak long-horizon accounting of update and maintenance debt | State Evolution and Reuse / From Updates to Long-Horizon Memory | TBD | integrated |
| karpukhin2020dpr | retrieval memory | dense passage vector index | retriever encoding and ANN lookup | retrieval quality vs index maintenance | top-k retrieval quality and E2E QA | update cadence under drift | State Evolution and Reuse / Structured Memory | TBD | integrated |
| khattab2020colbert | retrieval memory | token-level late-interaction index | offline encoding + online late interaction | quality-cost tradeoff in retrieval path | effectiveness with lower FLOPs | memory footprint and update overhead | State Evolution and Reuse / Structured Memory | TBD | integrated |
| li2024keldar | KG-enhanced memory | decomposed KG retrieval state | question decomposition and atomic retrieval control | structured reasoning quality vs retrieval overhead | answer quality + retrieval efficiency | maintenance of structured memory under drift | State Evolution and Reuse / Structured Memory | TBD | integrated |
| li2025global | KG-enhanced memory | planned KG retrieval state | global planning and atomic retrieval | structured retrieval control vs latency overhead | answer quality + reasoning stability | lifecycle maintenance under drift | State Evolution and Reuse / Structured Memory | TBD | integrated |
| asai2024selfrag | retrieval control | self-reflective retrieval trace | planner-mediated retrieval gating | retrieval benefit depends on token cost and memory freshness | answer quality + inference cost | weak coupling to index-maintenance triggers | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| guu2020realm | retrieval memory | retriever-coupled document memory | retrieval-augmented pretraining and memory refresh | corpus freshness and retriever drift | downstream QA quality | online maintenance semantics remain weak | State Evolution and Reuse / Structured Memory | TBD | integrated |
| lewis2020rag | retrieval memory | external passage memory | retrieval-generation coupling and document conditioning | retrieval latency vs answer quality | knowledge-intensive QA quality | index lifecycle mostly offloaded from the runtime | State Evolution and Reuse / Structured Memory | TBD | integrated |
| borgeaud2022retro | large external memory | chunk-level token datastore | large-scale retrieval over frozen external memory | retrieval bandwidth and memory scale | perplexity and downstream task quality | costly refresh and maintenance pipeline | State Evolution and Reuse / Structured Memory | TBD | integrated |
| izacard2022atlas | retrieval memory | learned retriever plus indexed corpus | few-shot retrieval orchestration | training-serving coupling and corpus dependence | few-shot task quality | weak explicit control over index maintenance | State Evolution and Reuse / Structured Memory | TBD | integrated |
| zhang2026flowrag | dynamic retrieval memory | evolving retriever memory and update trace | continual retriever adaptation with refresh control | corpus drift and reuse-value decay | long-horizon retrieval quality | planner/index co-scheduling remains weak | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| wang2026candor | continuous ANNS | dynamic ANN index and stream update state | online ANN maintenance and rebuild evaluation | churn, freshness, and recall-latency drift | long-horizon recall-latency stability | unified policies for rebuild and retention | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| jegou2011pq | ANN infrastructure | compressed vector-codebook index | quantization and memory-footprint control | memory reduction vs search distortion | recall-speed-memory tradeoff | dynamic update cost under evolving corpora | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| malkov2020hnsw | ANN infrastructure | graph-structured nearest-neighbor index | graph connectivity and search-expansion control | latency/recall tradeoff under graph growth | ANN recall and query latency | maintenance debt under frequent updates | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| singh2021freshdiskann | streaming ANN infrastructure | SSD-backed mutable proximity graph | online pruning, repair, and disk-aware search control | freshness depends on graph maintenance locality and storage-tier behavior | recall + latency + update throughput | deletion and rebuild policy remain exposed under heavy churn | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| wang2021milvus | vector data management system | segmented distributed vector collections and metadata | segment lifecycle, indexing, and distributed query routing | maintenance and serving interact through segment sealing and background rebuilds | distributed throughput + query latency | runtime compaction and freshness triggers remain weak | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| chen2024singlestorev | vector database / integrated ANN store | segment-scoped vectors, per-segment indexes, cross-segment indexes, and delete bitmaps | index construction and merging, auto index selection, Top() pushdown, and segment-level search orchestration | SQL optimizer and storage engine jointly determine which vector-state shards are searched, filtered, and merged | QPS-recall + index build time + mixed-query performance under cluster scaling | weak guidance on update-heavy maintenance, merge triggers, and memory footprint under churn | Retrieval Memory and Vector-Index State Layers | TBD | note-ready |
| quinn2025accelerating | RAG retrieval acceleration / vector memory | large external embedding stores and exact top-k retrieval state | exact-search offload, near-memory placement, and retrieval batch/K control | retrieval quality and exact-search latency directly constrain generation accuracy, end-to-end latency, and prompt-side GPU cost | end-to-end RAG latency + throughput + accuracy at large corpus scale | online updates, deletion/rebuild policy, and multi-node retrieval governance remain weak | Retrieval Memory and Vector-Index State Layers | TBD | note-ready |
| ray2025metis | quality-aware RAG serving | per-query RAG configuration choices plus GPU-memory budget state | LLM-profile-guided configuration pruning and memory-aware query scheduling | query profile reshapes chunk count, synthesis path, and memory occupancy, which then changes latency-quality tradeoffs under load | F1 + end-to-end latency + throughput across multi-dataset RAG workloads | retrieval-memory maintenance, index freshness, and planner-to-index lifecycle triggers remain outside the design | Retrieval Memory and Vector-Index State Layers | TBD | context-candidate |
| zhang2024rummy | vector query processing / vector-index state | IVF clusters, per-query cluster access plans, and GPU residency state for balanced clusters | cluster retrofitting, transfer reordering, grouping, and dynamic kernel padding | cluster residency and reordered transfer/compute overlap determine PCIe hiding, SM utilization, and billion-scale query cost | QPS + latency-recall + cost efficiency for vector search beyond GPU memory | limited to IVF-style independence, with weak online update and multi-GPU index governance | Retrieval Memory and Vector-Index State Layers | TBD | note-ready |
| guo2022manu | cloud-native vector database | partitioned vector shards and cloud storage hierarchy | distributed indexing, concurrency control, and elastic query execution | query isolation depends on background maintenance and shard placement | scalability + latency + operability | planner-visible maintenance signals remain weak | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| xu2023spfresh | billion-scale vector index | in-place disk-resident graph state | incremental in-place update and locality-preserving maintenance | storage hierarchy and mutation path constrain freshness at scale | billion-scale recall-latency-update balance | planner-visible refresh triggers remain absent | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| xu2025place | deletion-aware streaming ANN | mutable graph index with in-place update path | in-place deletion and graph-local repair | deletion semantics trade index compactness against update overhead | recall drift + update cost | end-to-end rebuild policy still unresolved | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| wang2024hipporag | retrieval memory | KG + graph memory substrate | graph-based retrieval orchestration | long-term memory structure and drift | quality-cost-latency | dynamic maintenance policies | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| sarthi2024raptor | hierarchical retrieval memory | tree-organized abstracted corpus state | recursive abstraction and hierarchical retrieval | retrieval depth vs maintenance complexity | answer quality and multi-hop retrieval | online refresh and compaction remain unclear | Retrieval Memory and Vector-Index State Layers | TBD | integrated |
| huang2025tigon | CXL-enabled distributed OLTP | cross-host active tuples plus CXL-resident lock, index, and coherence metadata | tuple migration, SWcc coherence, CXL-index lookup, and local commit control | CXL atomic shared-memory access replaces network message rounds and reshapes cross-partition concurrency, ownership, and commit latency | throughput + latency under cross-partition OLTP and bounded HWcc memory | small-pod CXL assumptions, SS2PL-only design, and weak portability beyond specialized shared-memory hardware | State Access and Scheduling / Runtime Control | TBD | note-ready |
| sun2025mepipe | memory-efficient LLM training / slice-level pipeline scheduling | slice-grained activation memory, per-slice forward/backward scheduling state, and deferred weight-gradient work queues | sequence pipeline parallelism, virtual pipeline parallelism, SVPP schedule selection, and fine-grained weight-gradient placement into bubble slots | activation lifetime and pipeline-bubble structure are jointly determined by slice granularity, so earlier backward start both shrinks memory pressure and reshapes pipeline utilization | end-to-end iteration time + activation-memory reduction + cost efficiency on commodity-GPU clusters | schedule search still relies on grid search, SPP reduces operator efficiency at higher split factors, and reliability/checkpoint integration remains weak | State Access and Scheduling / Runtime Control | TBD | note-ready |
| zeuch2020nebulastream | edge-cloud stream systems | geo-distributed sensor, operator, and transactional state | adaptive sensor handling, heterogeneous execution, and end-to-end placement control | cloud-external state movement couples scalability, latency, and consistency | complex analytics beyond the cloud | runtime contracts for geo-distributed state ownership remain weak | Broader Literature Landscape | TBD | integrated |
| gao2026grace | dynamic graph processing | PMA/CSR layout state with reserved space and rebalancing metadata | property-guided reservation and cousin-aware rebalancing | reconstruction policy shapes future update cost and locality | reconstruction time + update throughput + compute efficiency | broader cross-domain ownership and disturbance semantics remain weak | Broader Literature Landscape | TBD | integrated |
| humphries2025wave | SmartNIC resource-management offload | scheduling, page-mapping, and RPC dispatch decision state plus host-SmartNIC queue state | queue-type selection, MMIO/DMA transport, prestaging, and prefetch | control decisions traverse PCIe, so transport choice reshapes tail latency and host-core reclamation | tail latency + host-core release for OS resource-management functions | manages control-plane decision state rather than reusable data state, so transfer to the survey core is indirect | Broader Literature Landscape | TBD | context-candidate |
| alquraan2024lolkv | RDMA-replicated key-value storage | replicated ObjectEntry segments, hash-table visibility pointers, and leader-election state | logless direct replication, segment ownership transfer, hash-table CAS visibility, and consolidation control | removing the replicated-log layer changes write serialization, follower CPU involvement, and recovery path under RDMA | throughput + tail latency + recovery time for linearizable single-key KV operations | single-key, memory-only design leaves multi-key transactions, persistent durability, and broader runtime-governed lifecycle control unresolved | Broader Literature Landscape | TBD | context-candidate |
| guo2023cornus | storage-disaggregated transaction commit | per-participant vote logs, shared durable decision state, and transaction termination metadata in disaggregated storage | LogOnce conditional writes, coordinator-log elimination, and participant-driven abort/termination control | once transactional decision state is lifted into shared storage, commit latency and blocking behavior depend on storage-side conditional visibility rather than only coordinator liveness | distributed transaction latency + termination time under storage-disaggregated cloud DBMS execution | transaction-commit focus is indirect relative to model-serving state, but it sharpens the survey's broader point that shared storage can reassign coordination authority over runtime state | Broader Literature Landscape | TBD | context-candidate |
| zhao2024salus | secure CXL-expanded GPU memory | encrypted data pages, MAC/counter security metadata, and tag-tracked metadata residency across GPU HBM and CXL memory | metadata-data co-migration, opportunistic metadata prefetch, and tag-driven secure metadata caching | CXL memory expansion is only deployable when confidentiality/integrity metadata can follow migrated data without turning remote security checks into the dominant latency source | performance overhead + metadata traffic under secure CXL-expanded GPU memory | security-centric design sharpens deployability constraints but does not itself govern runtime placement, eviction, or multi-GPU ownership control | Broader Literature Landscape | TBD | context-candidate |
| hu2024aceso | disaggregated KV-store fault tolerance | checkpointed index state, erasure-coded KV blocks, and recovery-version metadata in memory-disaggregated KV stores | hybrid checkpoint/erasure-coding policy, differential checkpointing, and tiered recovery control | fault tolerance in disaggregated memory depends on separating index-state recovery from bulk KV recovery so update IOPS and storage overhead are not both dominated by replication | throughput + tail latency + memory overhead + recovery time in memory-disaggregated KV stores | storage-centric system with limited direct LLM lifecycle semantics and dependence on RDMA-era design assumptions | Broader Literature Landscape | hu2024aceso | note-ready |
| zhong2024unimem | disaggregated memory hierarchy | shadow-region address mappings, tiered host/device local-cache pages, and remote-memory placement metadata | unified local-remote hierarchy, filter-cache replacement, and fully-used page promotion between host and device-attached memory | remote-memory performance depends on eliminating extra translation and preventing cache thrashing and pollution across local tiers | average memory access time + data amplification under cache-coherent disaggregated memory | requires cache-coherent accelerator support and remains indirect relative to model-serving state despite strong tiered-memory lessons | Broader Literature Landscape | zhong2024unimem | note-ready |
| lee2023waltz | storage-tail-latency control on ZNS SSD | WAL append position, zone-replacement state, and lazily updated ZoneFile metadata | zone-append-based concurrent WAL writes, background zone reservation, and lazy metadata update control | hardware append ordering replaces host-side leader election, so device-level synchronization semantics directly reshape tail latency in persistent-state paths | p99 write latency + throughput + write amplification on ZNS-backed LSM trees | storage-specific example rather than a direct LLM runtime mechanism, but useful as a broader state-synchronization analogue | Broader Literature Landscape | TBD | context-candidate |
| jalalian2024extmem | application-aware virtual memory management | user-space-managed virtual pages, upcall-delivered fault state, and policy-visible access telemetry | signal-based user-space fault handling, programmable paging policy, and application-aware prefetch/reclaim control | external paging only pays when fault-handling overhead is low enough for application-specific policies to exploit structure better than generic kernel VM heuristics | runtime under constrained memory + paging overhead for data-intensive applications | depends on kernel support and remains a general VM framework rather than a direct model-serving runtime design | Broader Literature Landscape | jalalian2024extmem | note-ready |
| raybuck2021hemem | tiered memory management on real NVM | per-page heat/read-write state across DRAM and Optane NVM tiers | PEBS-driven hot-page sampling, write-aware placement, and rate-limited asynchronous migration | real NVM only approaches DRAM-like behavior when sampling overhead, migration bandwidth, and write asymmetry are jointly managed | throughput + tail latency + NVM writes under real tiered-memory workloads | benefits weaken on uniform-access workloads and the design is tied to platform-specific sampling and kernel support | Broader Literature Landscape | raybuck2021hemem | note-ready |
| cho2024coaxial | CXL-centric memory-system design | memory-channel bandwidth budget, LLC miss traffic, and queueing state across many-core servers | CXL-for-DDR replacement, CALM overlap control, and asymmetric RX/TX provisioning | effective memory latency depends on whether extra serial-link delay is outweighed by reduced queueing under much higher aggregate channel bandwidth | system speedup + effective memory latency + energy-delay tradeoff under memory-intensive server workloads | evidence is simulation-based and indirect relative to model-serving state, with notable power cost and some workload regressions | Broader Literature Landscape | cho2024coaxial | note-ready |
| zhao2025equilibria | fair multi-tenant CXL memory tiering | per-container local-versus-CXL page residency, protection bounds, and thrashing state | cgroup-level lower protection and upper bound control, demotion/promotion rate shaping, and thrashing suppression | in multi-tenant tiered memory, one workload's page promotions and fast-tier usage directly reshape neighbors' latency and effective local-memory share | throughput + tail latency + fairness under production CXL-tiered deployments | requires tuned protection bounds and focuses on capacity fairness more than joint bandwidth or accelerator-memory fairness | Broader Literature Landscape | zhao2025equilibria | note-ready |
| peng2024scalacache | storage/cache-management offload | page-cache metadata, HMB-resident cached pages, and fused LPN-to-page-location mappings across host memory and CSD flash | FusionFTL, CSD-offloaded cache management, Queue Index lookup, and GC-aware replacement | host-side page-cache overhead depends on whether address translation, queueing, and GC coordination are pushed down toward the storage device | bandwidth + latency + CPU overhead under user-space page cache workloads | depends on computational-storage support and targets storage-page state rather than directly reusable serving memory | Broader Literature Landscape | peng2024scalacache | note-ready |
| hao2020linnos | SSD latency predictability and storage-aware failover | per-I/O slow-versus-fast latency state, recent I/O-history features, and submit/revoke decision state at the block layer | offline inflection-point labeling, lightweight neural slow-I/O prediction, biased false-negative-aware training, and hedging-assisted admission control | storage tail latency only improves when block-layer prediction is fast enough to revoke likely-slow I/Os before submission and reroute them across replicas | average and tail I/O latency + prediction accuracy + failover overhead under replicated SSD deployments | depends on replica-backed storage and heavy-tailed SSD latency distributions, making the connection to runtime-managed model state indirect but still useful as a systems analogue | Broader Literature Landscape | hao2020linnos | context-candidate |
| wang2025finemem | fine-grained disaggregated memory management | RDMA-exposed remote chunks, two-layer bitmap allocation metadata, capability-table keys, and embedded redo-log state across compute and memory nodes | whole-pool pre-registration, MW-based capability rebinding, bitmap-tree allocation, contention-aware span selection, and CAS-coupled crash-consistent allocation control | disaggregated memory pooling only becomes efficient when fine-grained allocation, isolation, and crash consistency can be enforced without reintroducing memory-node CPU bottlenecks or MR-registration latency on the critical path | allocation latency + CAS retry rate + memory utilization + end-to-end throughput for KV, swap, and malloc-style workloads on RDMA memory pools | depends on RDMA/MW hardware assumptions, leaves memory-node failure handling open, and still lacks stronger coherence and larger-scale pool governance semantics | Broader Literature Landscape | wang2025finemem | note-ready |
| chang2024gmt | GPU-orchestrated memory tiering | 64KB page residency state across GPU memory, host memory, and SSD plus reuse-distance-class metadata for evicted pages | RRD-class-based tier placement, VTD-guided regression, Markov-style reuse prediction, and hybrid GPU-managed transfer-path selection across tiers | tier-placement decisions only pay off when page reuse class and transfer mechanism jointly reduce SSD traffic without reintroducing CPU-side orchestration bottlenecks | runtime speedup + I/O reduction under single-GPU oversubscribed memory workloads with GPU-managed three-tier hierarchy | infrastructure-focused and indirect to model-serving runtimes, with single-GPU scope and unresolved portability to multi-GPU or LLM-specific state lifecycles | Broader Literature Landscape | TBD | note-ready |
| chang2025llmulator | accelerator cost modeling for input-adaptive control flow | predicted power, area, flip-flop, and cycle-cost state for candidate accelerator-program-input triples | progressive numeric modeling, DPO-based dynamic calibration, and synthetic-data-driven cost prediction control | design-space exploration quality depends on whether predicted performance state remains accurate under dynamic control flow and unseen hardware mappings | MAPE across power/area/FF/cycle prediction + inference cost for accelerator design exploration | design-time prediction rather than runtime state control makes it analytically indirect, and inference overhead plus difficult programs still limit deployment value | Broader Literature Landscape | chang2025llmulator | context-candidate |
| tabatabai2024fbmm | extensible memory management | kernel page mappings, virtual-address allocation state, and policy-managed placement metadata recast as memory-management filesystem state | VFS-backed memory-management callbacks, per-policy MFS modules, asynchronous page scanning/migration, and address-allocation control | memory-tiering policy only becomes deployable when placement, paging, and translation control can be inserted without invasive kernel rewrites across the MM path | allocation overhead + throughput under tiered memory, bandwidth scaling, and contiguous-allocation workloads | policy composition remains weak, kernel memory stays outside scope, and evidence relies on NUMA-style emulation rather than production CXL deployment | State Access and Scheduling / Runtime Control | TBD | note-ready |
| zhong2024memstrata | virtualized CXL memory tiering | VM page residency across local DRAM and CXL memory plus tenant slowdown and fast-tier allocation state | hardware-managed flat-memory tiering, page-coloring-based interference control, slowdown estimation, and dedicated local-DRAM reservation for outliers | hardware tiering handles most movement, but tenant interference and mixed access patterns still couple local-memory allocation to latency outliers and fairness | slowdown under 1.5x memory expansion across large virtualized workload suites | depends on Intel-style hardware support and single-server evaluation, leaving cross-node CXL pooling and accelerator-memory coordination open | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| lyerly2025skybridge | distributed cache freshness control | partition-level freshness watermarks and stale/not-stale cache metadata under asynchronous replication | gap-detecting side replication, HLC watermark propagation, and stale-read-triggered refresh control | freshness depends on whether lightweight stale signals can outrun the tail of ordered replication and trigger selective repair before stale reads escape | bounded-staleness consistency rate + deployment overhead in production distributed caches | only detects staleness instead of carrying fresh state, so transfer to model-serving state remains indirect | Broader Literature Landscape | TBD | context-candidate |
| lin2025whatifstragglers | distributed large-model training diagnostics | per-GPU kernel timelines, per-iteration stage timing, sequence-length skew, and straggler attribution state | kernel-level trace collection, what-if simulation, and counterfactual removal of PP imbalance, sequence skew, hardware heterogeneity, and GC stalls | synchronous training couples the slowest worker's delay to the whole job, so root-cause attribution depends on reconstructing how stage imbalance and runtime stalls propagate through barriers | straggler prevalence + attributable slowdown across production large-model training jobs | framework-specific offline diagnosis leaves online mitigation and cross-stack portability unresolved | State Access and Scheduling / Runtime Control | TBD | note-ready |
| zhu2025nanoflow | LLM serving / intra-device pipeline scheduling | nano-batch execution state, per-op resource allocation state, and local KV/offload state | nano-batch partitioning, overlap scheduling, and resource-share assignment | compute-memory-network overlap inside one device governs throughput more than request-level state control | tokens per second per GPU + normalized latency | state lens stays device-local, with weak cluster-scale ownership, admission, and lifecycle semantics | Broader Literature Landscape | TBD | context-candidate |
| khare2025superserve | adaptive inference serving | shared SuperNet weights, SubNet activation path, slack state, and profiled latency-accuracy buckets | SubNet actuation plus slack-aware reactive scheduling | actuation delay and bursty workload shifts couple model-choice granularity to SLO attainment and served accuracy | accuracy + SLO attainment under bursty serving workloads | depends on SuperNet training and remains weak on LLM-specific short-lived memory governance | Broader Literature Landscape | TBD | context-candidate |
| gao2025rethinkingkvcompression | KV-cache compression evaluation | compressed KV-cache representations, output-length shifts, and sample-level failure patterns under production serving stacks | compression-evaluation framing across throughput analysis, response-length prediction, and negative-sample inspection | practical value of compression depends on interaction with FlashAttention/PagedAttention, changed generation length, and tail failure concentration rather than nominal compression ratio alone | throughput + end-to-end latency + negative-sample rate across serving frameworks | meta-evaluation paper rather than a new control mechanism, so it sharpens the survey's cautionary lens more than it adds a deployable runtime design | Broader Literature Landscape | TBD | context-candidate |
| li2024streamcache | stream-oriented page-cache management on fast storage | scan-stream intervals, page-cache residency metadata, dirty/writeback state, and per-core free-page pools for buffered file scanning | stream-tracking tree insertion/merge, stream-grained reclaim/writeback, and two-layer per-core/global page allocation | buffered I/O on fast storage stalls once page-granular metadata and writeback locks dominate, so throughput only scales when sequential scan state is lifted from per-page control to stream-level control | I/O bandwidth + writeback interference + checkpoint/restart time under file-scanning and AI checkpoint workloads | largely limited to sequential scan workloads and XFS-style kernel modifications, with weaker answers for mixed random access or portable runtime integration | Broader Literature Landscape | li2024streamcache | note-ready |
| qiu2025hotrap | hot-record governance in tiered LSM storage | record-grain hotness scores, RALT access-history state, and promotion-buffered records spanning fast and slow tiers | record-level hotness tracking, hotness-aware compaction retention, flush-triggered promotion, and self-tuned RALT capacity control | tiered storage only preserves read performance when hot records can be retained or promoted at record granularity before compaction cadence or coarse-object movement lets hot windows expire | throughput + tail latency + metadata I/O overhead under YCSB and production-trace tiered LSM workloads | RALT adds nontrivial write/read amplification and remains storage-specific, leaving transfer to KV-cache or broader runtime-managed short-lived state as an analogy rather than a deployed control plane | Broader Literature Landscape | qiu2025hotrap | note-ready |
| jiang2025shadowviews | disaggregated block storage telemetry | EBOF-internal I/O pipeline state, SSD-local congestion state, and elastic volume bandwidth allocations | Shadow View telemetry, eIO scheduling, and bandwidth-auction-based allocation control | client-visible storage throughput depends on whether placement and scheduling respond to hidden enclosure-level congestion rather than only host-visible demand | single-volume bandwidth + object-store throughput under elastic EBOF deployments | multi-tenant isolation and cross-cluster ownership semantics remain thin despite strong telemetry exposure | State Access and Scheduling / Runtime Control | jiang2025shadowviews | note-ready |
| vuppalapati2020snowflake | elastic query execution on disaggregated storage | persistent warehouse data, ephemeral intermediate data, and virtual-warehouse execution state | compute-storage disaggregation, virtual-warehouse elasticity, and query scheduling over remote intermediate state | elasticity only pays when transient execution state can move across decoupled compute while cache hit rate and network cost remain controlled | elasticity range + CPU efficiency + cache-hit rate under cloud data-warehouse workloads | temporary-state locality remains partly coupled to compute and weakens under finer-grained billing or more extreme network asymmetry | State Access and Scheduling / Runtime Control | vuppalapati2020snowflake | note-ready |
| jin2024concealing | compression-accelerated HPC I/O scheduling | compute-side idle windows, compression tasks, and staged I/O request state | in-situ task scheduling, pipeline splitting, and Johnson-style ordering for compression and I/O tasks | compression only hides I/O when task placement matches barrier-induced slack rather than extending the critical path | I/O overhead reduction + end-to-end slowdown under iterative HPC applications | depends on predictable iteration structure and leaves portability to dynamic training/runtime pipelines unresolved | Hardware-Conscious and Approximation-Aware Stateful Execution | jin2024concealing | note-ready |
| hu2021ecwide | wide-stripe erasure coding in distributed storage | data/parity block placement, locality metadata, and repair-path state across racks and cache levels | wide-stripe parameter selection, two-step local repair, and locality-aware data placement | coding efficiency only survives at wide stripe widths when block locality reduces both repair traffic and CPU cache disruption | repair time + redundancy + throughput under KV-style distributed storage workloads | rehashing and partial-update overhead remain open under hotter or more dynamic key-value paths | Broader Literature Landscape | hu2021ecwide | note-ready |
| kwon2020fvm | FPGA-assisted storage virtualization | VM-visible virtual queues, remapped doorbells, and emulated NVMe completion state | FPGA-side virtual device emulation, SR-IOV-assisted remapping, and queue-management offload | storage virtualization overhead becomes scalable only when queue-state manipulation is removed from host software on the critical path | throughput + latency + multi-device scaling under virtualized storage workloads | FPGA cost and on-chip resource ceilings bound scalability and weaken portability to commodity deployments | Hardware-Conscious and Approximation-Aware Stateful Execution | kwon2020fvm | note-ready |
| jha2025hycache | DNN preprocessing cache placement | intermediate preprocessing tensors, cache-residency metadata, and stage-level reuse state across DRAM and SSD | ILP-guided hybrid cache placement and reuse-aware admission across preprocessing stages | pipeline acceleration depends on matching reuse value and recomputation cost to tier capacity rather than caching whole stages blindly | preprocessing throughput + storage overhead under DNN input pipelines | offline profiling and ILP solve time constrain adaptation under rapidly shifting pipelines or data mixes | Hardware-Conscious and Approximation-Aware Stateful Execution | jha2025hycache | note-ready |
| huang2025obscura | bubble-filling recomputation control in LLM training | early-stage activations, pipeline-bubble slack, and swap/recompute coordination state | bubble-filling pipeline transformation, dependency relaxation, and swap-aware recomputation scheduling | recomputation only becomes cheap when pipeline bubbles absorb it without creating new inter-stage imbalance or transfer stalls | training throughput + recomputation overhead under pipeline-parallel LLM training | benefits depend on stable bubble structure and still leave broader fault tolerance and adaptive repartitioning open | State Evolution and Reuse / From Updates to Long-Horizon Memory | huang2025obscura | note-ready |
| zuo2021race | RDMA-conscious hashing on disaggregated memory | remote directory state, subtable metadata, and bucket occupancy under one-sided RDMA | associativity-aware subtable layout, CAS-based lock-free updates, and SRCD caching | constant-time remote hashing depends on shrinking network round trips and keeping consistency metadata colocated with lookup paths | throughput + insertion latency under YCSB-style disaggregated-memory KV workloads | metadata overhead and limited atomic support leave broader CXL-style portability unresolved | State Access and Scheduling / Runtime Control | zuo2021race | note-ready |
| li2020pegasus | in-network coherence for skewed distributed storage | hot-object replica locations, coherence-directory metadata, and versioned routing state | in-network coherence directories, version-based synchronization, and hot-object replication control | hot-key mitigation only scales when replica placement and coherence state move with rack-local topology instead of host-only coordination | throughput + hotspot tolerance under skewed distributed-storage workloads | cross-rack fault tolerance and broader deployment assumptions remain relatively narrow | State Access and Scheduling / Runtime Control | li2020pegasus | note-ready |
| domingo2021pfsck | filesystem-checking parallelization | inode and directory metadata, pass-level dependency state, and repair-task work queues | data-parallel scanning, pass pipelining, and dynamic thread scheduling across repair phases | repair only accelerates on fast storage when metadata traversal and pass ordering are parallelized without violating global consistency checks | fsck throughput + I/O bandwidth under modern storage devices | memory overhead and behavior on heavily corrupted filesystems remain less resolved | Hardware-Conscious and Approximation-Aware Stateful Execution | domingo2021pfsck | note-ready |
| cao2020polardb | computational-storage query offload | table-scan data streams, compressed tuples, and scan-filter execution state in storage-side FPGA logic | storage-side scan pushdown, decompression, and predicate evaluation control | offloading only helps when network and PCIe traffic reductions outweigh FPGA-side execution and format constraints | network traffic + PCIe traffic + query latency under analytical cloud-database workloads | support remains limited to simpler predicates and specific compression formats | Hardware-Conscious and Approximation-Aware Stateful Execution | cao2020polardb | note-ready |
| kumar2020quiver | informed storage caching for deep learning | shared training-dataset cache entries, content hashes, and per-job reuse-value state on local SSDs | informed admission/eviction, content-hash indexing, and coordinated cache sharing | distributed training only benefits from local SSD caching when replacement decisions reflect epoch structure and cross-job sharing value | single-job speedup + cluster throughput under deep-learning training traces | digest computation and reuse-value estimation add overhead and remain tied to particular training-access patterns | State Evolution and Reuse / From Updates to Long-Horizon Memory | kumar2020quiver | note-ready |
| ji2025cxl | CXL-based end-host networking rearchitecture | shared packet descriptors, buffer state, and NIC/CPU coherence metadata across host and device memory | CXL.cache and CXL.mem based queue/buffer placement and offloaded packet processing control | networking latency depends on whether coherence and memory sharing remove PCIe data-movement overhead without reintroducing synchronization bottlenecks | packet-processing latency + KVS latency under CXL-enabled end-host prototypes | prototype clock limits and weak atomic semantics leave broader production deployment open | Hardware-Conscious and Approximation-Aware Stateful Execution | ji2025cxl | note-ready |
| yang2021segcache | small-object in-memory cache management | TTL-bearing small-object segments, approximate frequency metadata, and segment-lifetime state | segment-grained allocation, batch expiration, and coordinated eviction over shared segments | small-object caching only remains memory efficient when metadata and expiration are amortized across segments instead of per object | memory efficiency + throughput under small-object cache workloads | approximate frequency tracking weakens exact recency control and leaves richer multi-tenant governance unresolved | Broader Literature Landscape | yang2021segcache | note-ready |
| chen2021spandb | hybrid-storage LSM KV engine | WAL state, SST placement, and hot/cold file residency across fast and cheap devices | SPDK-parallel WAL, selective hot-data placement, and asynchronous request pipelining | hybrid storage only improves the cost-performance frontier when log serialization and hot-data placement are jointly controlled | throughput + latency under hybrid-storage KV workloads | requires dedicated NVMe resources and still inherits cold-tier bandwidth limits | Broader Literature Landscape | chen2021spandb | note-ready |
| wang2018superneurons | dynamic GPU memory management for DNN training | tensor liveness state, offloaded activations, and recomputation candidates across GPU and host memory | liveness analysis, unified tensor pool, offload scheduling, and cost-aware recomputation control | training larger models depends on coordinating tensor lifetime, transfer, and recomputation rather than optimizing any one path in isolation | peak memory + trainability + runtime overhead under deep neural network training | additional recomputation cost and reliance on static graph analysis limit generality to more dynamic training stacks | State Evolution and Reuse / From Updates to Long-Horizon Memory | wang2018superneurons | note-ready |
| dong2024toleo | freshness metadata scaling with CXL and PIM | cache-line version metadata, freshness state, and version-cache residency over tera-scale memory | near-memory version storage, compressed version formats, and version-cache control | freshness only scales when metadata movement is compressed and decoupled enough not to dominate CXL-expanded memory accesses | freshness support overhead + metadata hit rate under trusted-memory prototypes | depends on trusted-memory assumptions and specialized CXL+PIM support, leaving broader security models and deployment portability unresolved | Hardware-Conscious and Approximation-Aware Stateful Execution | dong2024toleo | note-ready |
| flatstore20 | persistent-memory KV storage | log-structured KV records, small/large KV batching state, and persistent-memory write-path metadata | selective batching, PM-aware log structuring, and adaptive small/large object write control | PM-resident KV throughput depends on matching write granularity to device-level persistence and compaction costs | throughput + latency under YCSB on persistent memory | compaction and journal-cleanup overheads remain meaningful under sustained updates | Broader Literature Landscape | flatstore20 | note-ready |
| loongserve2024 | long-context LLM serving with elastic sequence parallelism | sequence-partitioned KV-cache state and elastic instance residency across long-context serving workers | elastic sequence parallelism, proactive scaling, and KV circulation-aware overlap control | long-context serving only scales when elastic partitioning and KV movement hide inter-GPU communication overhead | throughput + TTFT under long-context serving workloads | parameter replication and broader lifecycle governance beyond sequence partitioning remain unresolved | State-Aware Execution Optimization / KV-Cache Management | loongserve2024 | note-ready |
| zico21 | concurrent DNN training via GPU memory sharing | feature-map lifetimes, region-level memory elasticity, and time-shifted job-coordination state | region-based memory sharing, scrooge scheduling, and TimeShift overlap control | sharing only improves consolidation when training phases are shifted enough to avoid simultaneous feature-map peaks | throughput + GPU memory utilization under concurrent DNN training | limited multi-job coordination scope and runtime tracking overhead remain open | Hardware-Conscious and Approximation-Aware Stateful Execution | zico21 | note-ready |
| blockflex22 | storage harvesting on software-defined flash | ghost-vSSD allocations, flash-block ownership, and predicted bandwidth/capacity/lifetime state | software-defined flash allocation, prediction-guided harvesting, and best-fit vSSD placement control | storage harvesting only works when latent flash capacity and bandwidth can be reassigned without destabilizing tenant performance | utilization + tenant throughput under cloud storage harvesting workloads | approximation error and hardware dependency on software-defined flash support remain limiting factors | Broader Literature Landscape | blockflex22 | note-ready |
| calcspar23 | contract-aware cloud LSM storage | IOPS budget state, compaction queues, and cache hotness under cloud storage contracts | IOPS stabilization, prioritized compaction scheduling, and wave-aware cache control | contract compliance depends on whether compaction and cache decisions stay aligned with token-bucket-like cloud storage behavior | p99 latency spikes + throughput under cloud-backed LSM workloads | threshold sensitivity and workload-locality dependence remain open | Broader Literature Landscape | calcspar23 | note-ready |
| fsmoe25 | sparse MoE training-system scheduling | collective-communication dependencies, expert-stage flow state, and pipeline overlap metadata | four-case pipelined scheduling and solver-guided flow coordination across MoE collectives | sparse MoE throughput is gated by whether communication phases are overlapped according to dependency asymmetry rather than by expert sparsity alone | throughput across many multi-node MoE training configurations | hyperparameter sensitivity and limited joint optimization with other parallelism modes remain open | State Access and Scheduling / Runtime Control | fsmoe25 | note-ready |
| liu2020hercules | heterogeneity-aware recommendation serving | per-model performance-power tuples, partition placements, and server-packing state across heterogeneous hardware | profile-guided partition search, hardware-aware placement, and constrained packing control | heterogeneous recommendation serving only improves cluster capacity when model partitioning and packing decisions align with power/performance asymmetry | cluster capacity + power consumption under personalized recommendation serving | profiling overhead and modeling assumptions limit transfer to more dynamic serving stacks | Broader Literature Landscape | liu2020hercules | note-ready |
| park2025clap | chiplet-locality-aware GPU memory mapping | physical-frame placement, TLB-coalescing metadata, and chiplet-local memory-mapping state | segment-granularity mapping, locality analysis, and chiplet-aware address allocation control | multi-chip GPUs only realize locality gains when mapping decisions reduce cross-chiplet traffic without exploding translation overhead | throughput + memory footprint under mixed GPU workloads | hardware/modulo overhead and bounded savings limit generality | Hardware-Conscious and Approximation-Aware Stateful Execution | park2025clap | note-ready |
| zheng2024vmcu | MCU DNN inference memory coordination | segment-granular tensor lifetimes, circular-buffer occupancy, and kernel-optimization state | segment-level in-place overlap, circular-buffer coordination, and precise lifecycle-based allocation control | memory-constrained MCU inference only works for larger models when tensor overlap and kernel scheduling are jointly optimized | RAM footprint + energy under MCU DNN inference | guard regions and segmentation overhead bound maximum savings | Hardware-Conscious and Approximation-Aware Stateful Execution | zheng2024vmcu | note-ready |
| huang2025chimera | ownership-transfer mitigation in shared-storage cloud-native databases | page ownership metadata, primary-node placement, and transaction-phase state across multi-primary nodes | two-phase transaction scheduling, delay-fetch batching, and coroutine-based ownership-transfer reduction | shared-storage databases stall under high contention when ownership transfer frequency dominates remote coordination cost | throughput + ownership transfers under OLTP workloads | global phase coordination and high-contention latency bounds remain open | State Access and Scheduling / Runtime Control | huang2025chimera | note-ready |
| khan2025flstore | storage for federated-learning non-training workloads | FL metadata caches, workload-class state, and tiered serverless storage residency | workload-class-aware tiering, serverless compute placement, and metadata caching control | non-training FL workflows only remain cheap when metadata access patterns are separated from heavy training-state paths | end-to-end latency + cost under audit/debug/incentive FL workloads | memory caps and weaker support for previously unseen workload classes remain open | State Evolution and Reuse / From Updates to Long-Horizon Memory | khan2025flstore | note-ready |
| alhomssi2023snapshot | scalable snapshot isolation in storage engines | version chains, visibility metadata, and garbage-collection state in disk-based MVCC engines | OSIC commit control, Graveyard Index maintenance, and visibility-aware tuple management | mixed OLTP/OLAP stability depends on bounding version-visibility traversal and garbage-collection debt together | throughput stability + latency under mixed transactional/analytical workloads | generalization beyond the demonstrated engine and CSN-style alternatives remains limited | Broader Literature Landscape | alhomssi2023snapshot | note-ready |
| abebe2022tiresias | predictive autonomous storage and indexing | workload-access-pattern state, physical layout options, and adaptation-planning metadata | arrival prediction, cost regression, and predictive storage/index adaptation control | physical-format switching only helps when predicted access changes are timely enough to amortize reorganization cost | query latency + storage footprint under workload transitions | robustness under prediction error and applicability scope beyond profiled tables remain open | Broader Literature Landscape | abebe2022tiresias | note-ready |
| park2024lpddrpnm | CXL-PNM inference acceleration | LPDDR5X-resident transformer parameters and in-memory compute state exposed through CXL.mem | PNM offload, parameter placement, and near-memory execution control | TCO gains only materialize when parameter-transfer savings exceed PNM execution overhead and platform constraints | throughput + energy/TCO under transformer inference | remains tightly coupled to one LPDDR-CXL-PNM platform design and leaves broader programmability, scheduling integration, and portability open | Hardware-Conscious and Approximation-Aware Stateful Execution | park2024lpddrpnm | context-candidate |
| pan2025instattention | in-storage long-context attention offload | flash-resident attention/KV state, sparse-loading metadata, and FPGA-managed offload state | in-storage attention offload, sparse loading, and dual-indexing control | storage offload only helps when sparse filtering and near-data access hide PCIe bottlenecks without collapsing attention quality | throughput + long-context quality under offloaded inference | depends on specialized storage hardware and leaves broader online serving integration, flash wear behavior, and cluster-level ownership semantics open | Hardware-Conscious and Approximation-Aware Stateful Execution | pan2025instattention | context-candidate |
| zhou2024neomem | CXL-native memory tiering co-design | page access counters, migration thresholds, and hardware-profiler state for local/remote memory placement | Count-Min-Sketch-style profiling, adaptive thresholds, and migration control | CXL-native tiering only remains effective when profiling overhead stays below software page-walk costs while still following workload drift | p99 latency + cost efficiency under CXL-tiered memory | approximate counting, threshold tuning, and rapid workload shifts still bound robustness beyond the demonstrated hardware/software co-design | State Access and Scheduling / Runtime Control | zhou2024neomem | note-ready |
| reidys2023rackblox | rack-scale storage with network-storage co-design | SSD queue state, network-path telemetry, and wear-balance metadata across rack-scale storage nodes | I/O redirection, GC coordination, and network-aware wear-balancing control | rack-scale storage only improves tail behavior when network-state observation is precise enough to steer around SSD and path hotspots together | p99.9 latency + wear imbalance + throughput under rack-scale storage workloads | deployability still depends on network-telemetry support and leaves broader consistency and failure-management semantics open | Broader Literature Landscape | reidys2023rackblox | context-candidate |
| patke2025indigo | hardware memory disaggregation / network-aware page migration | per-page local/remote residency plus access-rate and burst-duration telemetry | network-aware migration thresholds learned by contextual bandit | migration payoff depends on network contention and remote transfer cost | runtime + network traffic on real hardware-memory-disaggregation prototype | arXiv-only status, heavy per-application training, and unclear portability to broader CXL serving stacks | Broader Literature Landscape | TBD | context-candidate |
| agrawal2024vidur | LLM inference simulation | deployment configuration state spanning parallelism strategy, scheduling policy, GPU SKU, and batch/chunk parameters | operator-triaged latency modeling, event-driven simulation, and cost-aware configuration search | configuration choice and workload trace jointly reshape queueing, replica utilization, and cost-performance frontier | latency prediction error + QPS/dollar search quality for LLM inference deployments | simulator ignores some CPU/runtime overheads and is not itself a serving control mechanism | Broader Literature Landscape | TBD | context-candidate |
| choi2024elasticrec | microservice-based elastic model serving for recommendation systems | hot/cold embedding-table shards, dense-model service state, and per-shard replica counts under skewed request demand | hotness-aware embedding partitioning, microservice-level elastic scaling, and DP-based memory-cost minimization for shard placement | serving cost depends on whether long-lived embedding state is split and replicated according to access skew rather than monolithic model copies | memory allocation + QPS per GB + deployment cost under recommendation serving workloads | recommendation-specific setting is indirect relative to LLM runtime state, but it sharpens the survey's broader lesson that state hotness and service elasticity should be co-designed | Broader Literature Landscape | TBD | context-candidate |
| rago2025 | RAG serving workflow optimization | DAG-structured submodel state, intermediate retrieval/rerank results, and pipeline-level batching/placement state | collocation placement, throughput allocation, and DAG-aware adaptive batching | submodel placement and batching determine cross-stage transfer cost, pipeline bubbles, and end-to-end RAG throughput | throughput + latency for multi-stage RAG serving pipelines | anonymous preprint status and weak canonical authorship prevent stable bibliographic integration | Broader Literature Landscape | TBD | context-candidate |
| domingo2025kamino | cache-aware cloud control-plane scheduling | per-agent allocator caches, request-class state, and queue/backlog state for VM placement services | latency-driven cache-aware request routing with per-agent delay estimation | dispatch decisions reshape both queueing delay and cache warmth, so scheduler state and cache state must be co-optimized rather than treated separately | average/p99 latency + cache-miss rate + memory footprint in production VM allocation | cloud-control-plane setting is indirect relative to runtime-managed model state, but it sharpens the survey's broader scheduling-with-state analogy | Broader Literature Landscape | TBD | context-candidate |
| weng2023fgd | GPU-sharing cluster scheduling under fragmentation | per-GPU fractional allocation state, stranded capacity, and workload-shape-dependent fragmentation statistics | fragmentation-gradient-guided placement plus best-fit tie breaking | every placement decision perturbs future schedulability because fractional GPU allocations create stranded capacity that bin-packing heuristics do not model well | unallocatable GPU count + effective utilization under production-style GPU-sharing traces | focuses on capacity fragmentation rather than reusable execution state, so it serves better as scheduling context than as a core runtime-state mechanism | Broader Literature Landscape | TBD | context-candidate |
| stripeless2025 | stripeless erasure-coded in-memory storage placement | erasure-coded data/parity shard placement, SBIBD-derived placement state, and failure-time reconfiguration constraints | local deterministic shard placement, SBIBD-guided placement-function control, and runtime placement-rule adjustment under failures | metadata-free placement only works when mathematical placement constraints preserve recoverability while removing centralized stripe coordination from the critical path | throughput + tail latency + recovery time + metadata overhead in in-memory erasure-coded storage | storage-centric setting is indirect relative to the survey core, and the note still leaves exact authorship/year normalization unresolved | Broader Literature Landscape | TBD | context-candidate |
| zhang2021intrawindowjoin | stream joins | window contents, partition state, and join-method metadata | execution-style, join-method, and partitioning selection | hardware topology and workload shape the feasible join frontier | throughput + latency across workload regimes | missing closed-loop join-plan switching under drift | State Access and Scheduling / Cost Modeling Under Locality and Topology | TBD | integrated |
| zhao2025morphstream | transactional stream processing | dependency graph, nondeterministic access metadata, and multiversioned window state | adaptive scheduling plus runtime dependency and window-state management | irregular access patterns reshape concurrency, semantics, and recovery cost together | throughput + latency under dynamic workloads | unified control across migration and recovery remains open | State Access and Scheduling / Runtime Control | TBD | integrated |
| wang2023dscstudy | stream clustering | summaries, windows, outlier state, and refinement state | design-space decomposition and algorithm-choice comparison | clustering accuracy and online efficiency depend on how maintenance dimensions are combined | clustering quality + processing efficiency | explicit runtime switching across clustering modules remains weak | State Evolution and Reuse / From Updates to Long-Horizon Memory | TBD | integrated |
| zhang2026neuromem | memory middleware | external memory entries plus insertion, consolidation, retrieval, and integration state | lifecycle decomposition and interleaved insertion-retrieval benchmarking | memory growth shifts latency and quality across the full lifecycle rather than one query step | token-level F1 + insertion/retrieval latency | missing unified control plane that turns lifecycle telemetry into online policy | State Evolution and Reuse / Structured Memory | TBD | integrated |
| liu2026sage | reasoning middleware | modular vector, window, and workflow state for LLM reasoning services | pluggable index layer, async updates, declarative windows, and dual-stream joins | reasoning quality and transparency depend on exposing workflow memory as controllable dataflow state | controllability + transparency + runtime efficiency | stable cross-workflow governance semantics remain open | State Evolution and Reuse / Structured Memory | TBD | integrated |
| yue2025marco | disk-based ANN | graph-layout state for disk-resident monotonic search paths | path-aware graph layout optimization and staged edge placement | edge layout determines future traversal locality and update locality under storage constraints | recall-preserving search efficiency + layout cost | integration with online rebuild and freshness triggers remains open | Retrieval Memory and Vector-Index State Layers | TBD | integrated |

### Cross-domain synthesis reused in Sections 5 and 7

The integrated rows above and below are also reused comparatively in Sections 5 and 7. Those sections do not add new literature clusters; they extract cross-cutting design principles, anti-patterns, case studies, and the forward-looking agenda from recurring control seams across streaming, serving, retrieval, approximation, and retention.

| kirkpatrick2017ewc | continual retention | protected parameter importance state | constrained parameter protection | forgetting from unconstrained updates | forgetting vs retention quality | weak systems model for distributed retention budgets | Continual Learning and Retention Governance | TBD | integrated |
| lopez2017gem | continual retention | episodic sample memory | gradient-constrained replay governance | interference between old and new tasks | forgetting vs adaptation quality | replay-store scaling and scheduling remain unclear | Continual Learning and Retention Governance | TBD | integrated |
| chaudhry2019agem | continual retention | sampled episodic buffer | approximate interference control | budgeted replay under online updates | adaptation cost vs forgetting | weaker retention-value modeling under long horizons | Continual Learning and Retention Governance | TBD | integrated |
| rolnick2019experience | continual retention | replay buffer state | experience admission and replay scheduling | memory budget vs future utility | task performance under continual streams | limited systems semantics for distributed memory stores | Continual Learning and Retention Governance | TBD | integrated |
| rebuffi2017icarl | continual retention | exemplar sets and class-mean memory | class-balanced exemplar admission and distillation | bounded memory couples representation drift and future classification quality | accuracy under incremental class growth | distributed retention-store semantics remain weak | Continual Learning and Retention Governance | TBD | integrated |
| aljundi2019mir | continual retention | interference-ranked replay memory | interference-aware sample ranking | sample usefulness depends on future gradient conflict | forgetting vs adaptation quality | ranking cost under large retention stores | Continual Learning and Retention Governance | TBD | integrated |
| buzzega2020der | continual retention | rehearsal buffer plus logits | distillation-aware replay governance | representation drift and label drift | continual accuracy across settings | storage and maintenance overhead under long runs | Continual Learning and Retention Governance | TBD | integrated |
| hayes2020remind | continual retention | compressed feature replay state | representation-level replay and compression control | feature compression vs retention fidelity | real-time continual accuracy | refresh and compaction cost under evolving streams | Continual Learning and Retention Governance | TBD | integrated |
| zhan2025assyllm | federated LLM fine-tuning via block reuse | block-level model-state pool, compatibility scores, adapters, and swap-managed assembled model state | forward-only block selection, elastic block adaptation, mixed-precision block compression, and correlation-aware block swapping | federated tuning under tight edge memory only scales when reusable block state can be selected, adapted, compressed, and swapped without full backpropagation | accuracy + memory footprint + local training time under federated fine-tuning | depends on curated block pools and leaves larger-model scaling, automatic pool construction, and adversarial robustness open | State Evolution and Reuse / From Updates to Long-Horizon Memory | zhan2025assyllm | note-ready |
| na2025flexinfer | phase-aware heterogeneous LLM inference | phase-specific execution plan, CPU/GPU layer placement, and PCIe transfer-resident model/KV state across prefill and decode | analytical policy estimation, per-phase strategy selection, and CPU-only/offload/split execution switching | prefill and decode stress compute throughput versus transfer avoidance differently, so device choice and transfer volume jointly reshape TTFT, TPOT, and end-to-end latency | end-to-end latency + TTFT + TPOT + PCIe traffic under memory-constrained single-GPU serving | relies on offline hardware profiling, coarse split planning, and single-GPU assumptions with limited cross-device or cluster-scale lifecycle control | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| wang2025wlbllm | long-context LLM training / 4D parallel load balancing | per-microbatch workload distribution, per-document sequence shards, and cross-GPU imbalance state across DP/PP/CP/TP training | variable-length packing, outlier-document delay, per-document sharding, and adaptive sharding selection | document-length skew and quadratic attention cost propagate imbalance across context and pipeline parallel layers, turning local shard skew into global training stalls | training throughput speedup under 32-256 GPU long-context training and large-model scaling | delay queues and sharding heuristics still depend on profiled workload structure and leave broader adaptive multi-cluster scheduling open | State Access and Scheduling / Runtime Control | TBD | note-ready |
| liu2025slimpipe | long-context LLM training / pipeline parallelism | slice-level activation state and attention-context exchange buffers across pipeline stages | uniform sequence slicing, slice-wise 1F1B scheduling, and context-exchange coordination across devices | activation lifetime and inter-device attention-context transfer jointly determine whether longer contexts fit without turning saved memory into communication stalls | activation-memory reduction + MFU + maximum supported context length under multi-GPU long-context training | gains depend on context-exchange efficiency and uniform slicing assumptions, with limited evidence for irregular sequence distributions or broader fault-tolerance integration | State Access and Scheduling / Runtime Control | TBD | note-ready |
| kim2025paise | heterogeneous GPU-PIM LLM inference | HBM-resident model weights and KV cache laid out for PIM-friendly access plus operator placement state across GPU and HBM-PIM cores | data-layout adjustment, offloading-incentive-based operator placement, and interleaved batched GEMV scheduling to PIM | decode-time bandwidth bottlenecks are relieved only when KV and weight access stay local to PIM banks and the remaining GPU/PIM handoff costs stay amortized | end-to-end inference latency reduction under real HBM-PIM hardware across model and batch regimes | depends on specialized Aquabolt-XL-class hardware, mainly benefits decode-phase GEMV paths, and leaves broader serving-stack integration open | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| dong2024qhitter | KV-cache compression / token selection | per-token KV cache entries plus attention-score importance and token-level quantization-sensitivity state | joint token-retention scoring over attention importance and quantization friendliness, followed by sparse-quantized KV retention | quantization perturbs attention rankings, so retention decisions that ignore quantization sensitivity preserve the wrong tokens and amplify quality loss under extreme KV compression | perplexity and task quality under 10x-40x KV compression across language modeling, QA, and streaming generation | extra profiling overhead, lambda tuning, and unclear page-level or distributed integration with serving runtimes remain open | State-Aware Execution Optimization / KV-Cache Management | TBD | note-ready |
| adnan2024keyformer | KV-cache reduction / key-token selection | token-level KV-cache entries partitioned into recent-window state and retained key-token state across autoregressive decoding | total KV budget, recent-window size, Gumbel-noise score construction, and temperature-scheduled key-token retention | retention policy jointly shrinks KV footprint, memory traffic, and decode latency while also constraining which historical semantics remain visible to later tokens | latency + generation throughput + quality under single-model decoder-only inference on summarization, conversation, and long-context workloads | remains single-request and inference-local, with no integration of paging, offload, cross-request reuse, or cluster-level KV ownership control | State-Aware Execution Optimization / KV-Cache Management | adnan2024keyformer | note-ready |
| liu2025vqllm | KV-cache vector quantization serving | VQ-compressed KV indices, codebooks, and GPU-resident decode buffers for reconstructed KV vectors | code-generation-driven VQ decode kernel synthesis, codebook caching, fusion strategy search, and incremental decode scheduling | VQ compression only becomes deployment-worthy when irregular codebook lookup and reconstruction overhead are lowered enough to avoid erasing the memory-capacity gains during decode | KV decode latency + serving throughput + perplexity under 4x-8x KV compression on GPU serving workloads | offline code generation cost, hardware-specific tuning, and weak support for adaptive or online-changing VQ layouts remain open | State-Aware Execution Optimization / KV-Cache Management | TBD | note-ready |
| liakopoulos2025maveriq | intent-based LLM serving deployment | deployment-configuration state mapping model layers, parallelism choices, and compression options onto fragmented GPU memory capacity | fingerprint-based profiling, analytical extrapolation, intent-aware configuration selection, and fragmentation-aware unbalanced pipeline placement | deployment-time layer placement and compression choices reshape latency, memory fit, and serving cost together, especially when fragmented cluster memory would otherwise strand usable capacity | profiling overhead + latency/cost prediction accuracy + cluster throughput/cost under multi-model serving deployment traces | assumptions are tied to repeated decoder blocks and largely homogeneous clusters, with limited validation for MoE-like architectures or dynamic post-deployment reconfiguration | State Access and Scheduling / Cost Modeling Under Locality and Topology | TBD | note-ready |
| jang2024smartinfinity | storage-offloaded LLM training | SSD-resident optimizer states and gradients plus near-storage update state inside computational storage devices | near-storage optimizer update offload, internal transfer overlap, and CSD-assisted top-K gradient compression | optimizer-state traffic dominates storage-offloaded training unless parameter updates are moved to the storage side and gradient writes are further compressed before crossing the host interconnect | training speedup + transfer reduction + multi-CSD scalability on real SmartSSD-based systems | depends on specialized CSD hardware, limited optimizer support, and leaves broader training-stack portability and long-run convergence effects under compression less resolved | Hardware-Conscious and Approximation-Aware Stateful Execution | TBD | note-ready |
| cao2025moelightning | memory-constrained MoE inference | offloaded MoE expert weights, CPU-resident KV/attention state, and microbatch pipeline state across GPU compute, CPU compute, and transfer stages | hierarchical roofline-guided scheduling, CGOPipe three-stage overlap, and paged weight transfer orchestration | throughput under memory-constrained MoE serving is bounded by how GPU compute, CPU attention, and CPU-GPU transfer overlap, not just by raw model sparsity or offload capacity alone | throughput under fixed GPU memory and CPU memory budgets for batch MoE inference | limited to single-node CPU-memory offload settings, with weak support for disk-tier offload, distributed multi-node coordination, or dynamic workload adaptation | LLM Serving and Short-Lived Memory Lifecycles | cao2025moelightning | note-ready |
| sanovar2024leanattention | decode-phase attention execution | tile-level partial attention state over KV-cache segments plus reduction-side softmax statistics during decode | hardware-aware lean-tile partitioning, CTA-level parallelization over context tiles, and reduction-based online-softmax merge | decode attention becomes underutilized on large-SM GPUs unless context work is split finely enough to keep SMs busy while still preserving exact softmax semantics | kernel speedup + end-to-end decode latency under long-context decode workloads on A100/H100-class GPUs | reduction overhead, offline tile tuning, and limited evidence for portability across broader hardware or multi-node serving stacks remain open | Hardware-Conscious and Approximation-Aware Stateful Execution | sanovar2024leanattention | note-ready |
| arfeen2024pipefill | pipeline-parallel LLM training / bubble reuse | predictable pipeline-bubble time slices, fill-task execution state, and preemptible auxiliary GPU work scheduled around training stages | bubble-window prediction, low-priority fill-task injection, and fast preemption/isolation between auxiliary work and primary training kernels | pipeline-parallel training wastes GPU cycles in deterministic bubbles, so auxiliary work only helps when it can be injected and revoked without disturbing the main training critical path | GPU utilization + auxiliary-task throughput + primary training throughput retention under pipeline-parallel training | assumes predictable bubble structure and preemptible low-state auxiliary work, with limited evidence for larger clusters or heavy data-movement fill tasks | State Access and Scheduling / Runtime Control | arfeen2024pipefill | note-ready |
| prabhu2020gdumb | continual retention | curated bounded sample memory | admission-only retention selection | admission policy dominates final performance under fixed budget | final retained-model quality | limited online service semantics and maintenance accounting | Continual Learning and Retention Governance | TBD | integrated |
| zhou2025ferret | online continual learning | bounded retention buffers and update pipeline state | memory-budget-aware retention and update coordination | memory pressure and update rate jointly determine future quality | continual accuracy under varying budgets | distributed retention-store semantics and maintenance debt remain weak | Continual Learning and Retention Governance | TBD | integrated |
| li2026streamfp | stream learning retention | fingerprint-ranked retained samples | representativeness-guided data selection and retention control | selection policy shapes future reuse value more than recency alone | continual quality under bounded retention | maintenance and refresh semantics for long-running retention stores remain weak | Continual Learning and Retention Governance | TBD | integrated |
| delange2021survey | continual learning survey | taxonomy-level retention abstractions | evaluation and mechanism categorization | benchmark choice shapes what forgetting or retention signals are visible | comparability across continual settings | systems-level operational metrics remain limited | Continual Learning and Retention Governance | TBD | integrated |

## Backlog Rows To Fill Next

| Bib key | Area | State object | Control surface | Coupling path | Evaluation boundary | Remaining gap | Target subsection in main.tex | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
