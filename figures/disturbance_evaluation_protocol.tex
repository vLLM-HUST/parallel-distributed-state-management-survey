\begin{tikzpicture}[
  node distance=7mm and 6mm,
  phase/.style={draw, rounded corners, thick, align=center, minimum width=24mm, minimum height=10mm, fill=black!3},
  metric/.style={draw, rounded corners, align=center, minimum width=28mm, minimum height=8mm, fill=black!1},
  arrow/.style={-{Latex[length=2.5mm]}, thick}
]

\node[phase] (warmup) {Warmup\\steady state};
\node[phase, right=of warmup] (burst) {Burst\\hotspot flip};
\node[phase, right=of burst] (reconfig) {Reconfigure\\migrate or fail};
\node[phase, right=of reconfig] (drift) {Drift\\quality shift};
\node[phase, right=of drift] (long) {Long horizon\\maintenance debt};

\draw[arrow] (warmup) -- (burst);
\draw[arrow] (burst) -- (reconfig);
\draw[arrow] (reconfig) -- (drift);
\draw[arrow] (drift) -- (long);

\node[metric, below=10mm of warmup] (m1) {throughput\\resource cost};
\node[metric, below=10mm of burst] (m2) {p99 latency\\conflict density};
\node[metric, below=10mm of reconfig] (m3) {recovery lag\\migration debt};
\node[metric, below=10mm of drift] (m4) {quality floor\\freshness};
\node[metric, below=10mm of long] (m5) {maintenance spikes\\reuse decay};

\draw[arrow] (warmup.south) -- (m1.north);
\draw[arrow] (burst.south) -- (m2.north);
\draw[arrow] (reconfig.south) -- (m3.north);
\draw[arrow] (drift.south) -- (m4.north);
\draw[arrow] (long.south) -- (m5.north);

\draw[arrow, bend left=18] (long.north) to node[above, align=center] {policy retuning\\or rebuild trigger} (warmup.north);
\end{tikzpicture}